<?xml version="1.0" ?>
<specification_set date-created="2026-05-19T11:31:53.118280-07:00" libspec-version="4.2.0">
  <specification type="BootstrapIntegrity" ref="spec.app.BootstrapIntegrity">
    <source target="BootstrapIntegrity" file="/usr/backup-working/work/libspec/spec/app.py"/>
    <docstring>libspec must spec itself using libspec.

The library's own spec/ directory must be kept up to date and at feature
parity with the actual capabilities of the library. This ensures that the
tool demonstrates the exact workflow it advocates for and that its own
development remains disciplined.</docstring>
    <context>
      <req_id>spec.app.BootstrapIntegrity</req_id>
      <title>BootstrapIntegrity</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.app.BootstrapIntegrity</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>libspec must spec itself using libspec.

The library's own spec/ directory must be kept up to date and at feature
parity with the actual capabilities of the library. This ensures that the
tool demonstrates the exact workflow it advocates for and that its own
development remains disciplined.</notes>
      <req_id>spec.app.BootstrapIntegrity</req_id>
      <title>BootstrapIntegrity</title>
    </delta_requirements>
  </specification>
  <specification type="LibSpec" ref="spec.app.LibSpec">
    <source target="LibSpec" file="/usr/backup-working/work/libspec/spec/app.py"/>
    <docstring>libspec is a spec-driven development library for LLM-assisted coding.

It provides a Python-native way to write, build, and diff
structured specifications. Specifications are authored as Python class
hierarchies where the class docstring is the canonical requirement text.

The library generates versioned XML artifacts from these specs that
serve as the source of truth for LLM-assisted code generation and
cross-referencing between requirements and source code.</docstring>
    <context>
      <req_id>spec.app.LibSpec</req_id>
      <title>LibSpec</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.app.LibSpec</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>libspec is a spec-driven development library for LLM-assisted coding.

It provides a Python-native way to write, build, and diff
structured specifications. Specifications are authored as Python class
hierarchies where the class docstring is the canonical requirement text.

The library generates versioned XML artifacts from these specs that
serve as the source of truth for LLM-assisted code generation and
cross-referencing between requirements and source code.</notes>
      <req_id>spec.app.LibSpec</req_id>
      <title>LibSpec</title>
    </delta_requirements>
  </specification>
  <specification type="PythonNativeAuthoring" ref="spec.app.PythonNativeAuthoring">
    <source target="PythonNativeAuthoring" file="/usr/backup-working/work/libspec/spec/app.py"/>
    <docstring>Specifications are written as ordinary Python classes.

No special DSL, no separate config files. A spec class is a Python class
that inherits from Ctx (or a Ctx-derived convenience base like Feature or
Requirement) and carries its requirements as a class-level docstring.

This means specs benefit from Python's class hierarchy and multiple
inheritance for composing cross-cutting concerns (e.g. error handling,
refactoring guidelines) into every requirement without repetition.</docstring>
    <context>
      <feature_name>PythonNativeAuthoring</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Specifications are written as ordinary Python classes.

No special DSL, no separate config files. A spec class is a Python class
that inherits from Ctx (or a Ctx-derived convenience base like Feature or
Requirement) and carries its requirements as a class-level docstring.

This means specs benefit from Python's class hierarchy and multiple
inheritance for composing cross-cutting concerns (e.g. error handling,
refactoring guidelines) into every requirement without repetition.</notes>
      <feature_name>PythonNativeAuthoring</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SpecDrivenDevelopment" ref="spec.app.SpecDrivenDevelopment">
    <source target="SpecDrivenDevelopment" file="/usr/backup-working/work/libspec/spec/app.py"/>
    <docstring>Specifications are the primary artifact of development.

Source code is generated from specifications, not the other way around.
Every component of a project should have a corresponding specification
class that describes its behavior, requirements, and constraints.
The spec is the contract; the implementation must satisfy it.

The workflow is:
1. Author or update spec classes in the spec/ directory.
2. Run `libspec build` to generate a versioned XML artifact.
3. Run `libspec diff` to surface what has changed since the last build.
4. Use the diff output as context for LLM-assisted code generation.</docstring>
    <context>
      <feature_name>SpecDrivenDevelopment</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Specifications are the primary artifact of development.

Source code is generated from specifications, not the other way around.
Every component of a project should have a corresponding specification
class that describes its behavior, requirements, and constraints.
The spec is the contract; the implementation must satisfy it.

The workflow is:
1. Author or update spec classes in the spec/ directory.
2. Run `libspec build` to generate a versioned XML artifact.
3. Run `libspec diff` to surface what has changed since the last build.
4. Use the diff output as context for LLM-assisted code generation.</notes>
      <feature_name>SpecDrivenDevelopment</feature_name>
    </delta_requirements>
  </specification>
  <specification type="VersionedXmlArtifacts" ref="spec.app.VersionedXmlArtifacts">
    <source target="VersionedXmlArtifacts" file="/usr/backup-working/work/libspec/spec/app.py"/>
    <docstring>Each `libspec build` run produces a content-hashed XML file.

The filename embeds a 20-character MD5 digest of the XML content so that
successive builds produce distinct, traceable artifacts. A date-created
timestamp is injected into the root element at write time.

The XML is human-readable (pretty-printed) and carries the libspec
version that generated it so that cross-version diffs can be detected
and rejected safely.</docstring>
    <context>
      <feature_name>VersionedXmlArtifacts</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Each `libspec build` run produces a content-hashed XML file.

The filename embeds a 20-character MD5 digest of the XML content so that
successive builds produce distinct, traceable artifacts. A date-created
timestamp is injected into the root element at write time.

The XML is human-readable (pretty-printed) and carries the libspec
version that generated it so that cross-version diffs can be detected
and rejected safely.</notes>
      <feature_name>VersionedXmlArtifacts</feature_name>
    </delta_requirements>
  </specification>
  <specification type="CtxBase" ref="spec.core.CtxBase">
    <source target="CtxBase" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>The Ctx base class provides the template rendering and XML serialization
engine that every specification class inherits.

Ctx-derived classes are discovered by `ctx_spec_classes_in_module()` which
filters module members to those whose `__module__` matches the module
being inspected (preventing re-emission of imported base classes).

Key properties:
- The class docstring is the specification text / Jinja2 template.
- Zero-argument public methods and attributes become template variables
  that are resolved automatically at render time.
- `ctx(template_only=True)` returns the dict of resolved template vars.
- `to_xml_element()` produces the full &lt;specification&gt; XML element.</docstring>
    <context>
      <req_id>spec.core.CtxBase</req_id>
      <title>CtxBase</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.core.CtxBase</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The Ctx base class provides the template rendering and XML serialization
engine that every specification class inherits.

Ctx-derived classes are discovered by `ctx_spec_classes_in_module()` which
filters module members to those whose `__module__` matches the module
being inspected (preventing re-emission of imported base classes).

Key properties:
- The class docstring is the specification text / Jinja2 template.
- Zero-argument public methods and attributes become template variables
  that are resolved automatically at render time.
- `ctx(template_only=True)` returns the dict of resolved template vars.
- `to_xml_element()` produces the full &lt;specification&gt; XML element.</notes>
      <req_id>spec.core.CtxBase</req_id>
      <title>CtxBase</title>
    </delta_requirements>
  </specification>
  <specification type="DeltaRequirements" ref="spec.core.DeltaRequirements">
    <source target="DeltaRequirements" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Delta requirements capture what a subclass adds beyond its parents.

`_delta_requirements()` computes the set of context fields and docstring
notes that differ from all ancestor classes. Only the deltas are included
in the &lt;delta_requirements&gt; XML element, keeping the diff output focused
on what is actually new.

The `notes` key is treated specially: if the instance docstring differs
from all inherited docstrings it is included as `notes`.</docstring>
    <context>
      <feature_name>DeltaRequirements</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Delta requirements capture what a subclass adds beyond its parents.

`_delta_requirements()` computes the set of context fields and docstring
notes that differ from all ancestor classes. Only the deltas are included
in the &lt;delta_requirements&gt; XML element, keeping the diff output focused
on what is actually new.

The `notes` key is treated specially: if the instance docstring differs
from all inherited docstrings it is included as `notes`.</notes>
      <feature_name>DeltaRequirements</feature_name>
    </delta_requirements>
  </specification>
  <specification type="DependencyStub" ref="spec.core.DependencyStub">
    <source target="DependencyStub" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Inherited superspecs that are not directly listed in modules() are
emitted as lightweight dependency stub elements.

A stub carries:
- type and ref attributes identifying the class.
- A dependency="true" marker.
- A template="true/false" flag indicating whether the docstring
  contains Jinja2 template syntax.
- The raw docstring_template text.
- A source element with file path and class name.
- An inherits element listing the stub's own parent refs.

Stubs enable the diff engine to detect changes to inherited specs even
when those superspecs live in a separate project or artifact.</docstring>
    <context>
      <feature_name>DependencyStub</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Inherited superspecs that are not directly listed in modules() are
emitted as lightweight dependency stub elements.

A stub carries:
- type and ref attributes identifying the class.
- A dependency="true" marker.
- A template="true/false" flag indicating whether the docstring
  contains Jinja2 template syntax.
- The raw docstring_template text.
- A source element with file path and class name.
- An inherits element listing the stub's own parent refs.

Stubs enable the diff engine to detect changes to inherited specs even
when those superspecs live in a separate project or artifact.</notes>
      <feature_name>DependencyStub</feature_name>
    </delta_requirements>
  </specification>
  <specification type="InheritanceResolution" ref="spec.core.InheritanceResolution">
    <source target="InheritanceResolution" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Ctx tracks the full MRO to correctly compute inherited context.

`_non_root_mro_classes()` returns all MRO classes excluding Ctx and object.
`_base_template()` concatenates docstrings from all ancestor classes
(innermost-first) into a single base template string so that inherited
requirements are visible alongside the subclass docstring.

`_inherited_field_values()` collects the return values of zero-argument
methods from each inherited Ctx class to detect field overrides.
`_detect_overrides()` compares the current instance context against
inherited values and tags fields that have been overridden.</docstring>
    <context>
      <feature_name>InheritanceResolution</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Ctx tracks the full MRO to correctly compute inherited context.

`_non_root_mro_classes()` returns all MRO classes excluding Ctx and object.
`_base_template()` concatenates docstrings from all ancestor classes
(innermost-first) into a single base template string so that inherited
requirements are visible alongside the subclass docstring.

`_inherited_field_values()` collects the return values of zero-argument
methods from each inherited Ctx class to detect field overrides.
`_detect_overrides()` compares the current instance context against
inherited values and tags fields that have been overridden.</notes>
      <feature_name>InheritanceResolution</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SourceInfoIntrospection" ref="spec.core.SourceInfoIntrospection">
    <source target="SourceInfoIntrospection" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Source file and line range are captured via Python introspection.

`inspect.getsourcefile()` and `inspect.getsourcelines()` are used to
locate each Ctx class in the filesystem at serialization time. The result
is stored in the &lt;source&gt; element and also used in error messages for
missing template variables.

If introspection fails (e.g. for dynamically generated classes), the
source element is omitted gracefully rather than raising an exception.</docstring>
    <context>
      <feature_name>SourceInfoIntrospection</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Source file and line range are captured via Python introspection.

`inspect.getsourcefile()` and `inspect.getsourcelines()` are used to
locate each Ctx class in the filesystem at serialization time. The result
is stored in the &lt;source&gt; element and also used in error messages for
missing template variables.

If introspection fails (e.g. for dynamically generated classes), the
source element is omitted gracefully rather than raising an exception.</notes>
      <feature_name>SourceInfoIntrospection</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SpecBase" ref="spec.core.SpecBase">
    <source target="SpecBase" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>The Spec base class is the entry point for a project's specification set.

Subclasses must implement `modules()` returning the list of Python modules
that contain Ctx-derived specification classes. `Spec` discovers all such
classes, instantiates them, and assembles them into a single XML document.

Key responsibilities:
- `generate_xml()`: Return the full specification as a pretty-printed XML string.
- `write_xml(output_dir)`: Write the hashed XML file.
- `handle_cli()`: Parse basic -o/--output and --xml flags for standalone use.</docstring>
    <context>
      <req_id>spec.core.SpecBase</req_id>
      <title>SpecBase</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.core.SpecBase</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The Spec base class is the entry point for a project's specification set.

Subclasses must implement `modules()` returning the list of Python modules
that contain Ctx-derived specification classes. `Spec` discovers all such
classes, instantiates them, and assembles them into a single XML document.

Key responsibilities:
- `generate_xml()`: Return the full specification as a pretty-printed XML string.
- `write_xml(output_dir)`: Write the hashed XML file.
- `handle_cli()`: Parse basic -o/--output and --xml flags for standalone use.</notes>
      <req_id>spec.core.SpecBase</req_id>
      <title>SpecBase</title>
    </delta_requirements>
  </specification>
  <specification type="TemplateRendering" ref="spec.core.TemplateRendering">
    <source target="TemplateRendering" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Specification docstrings are treated as Jinja2 templates.

Undeclared variables in the combined base+instance template are collected
via `jinja2.meta.find_undeclared_variables`. Each variable name is mapped
to a same-named (with hyphens replaced by underscores) method or attribute
on the Ctx instance.

If a required variable has no matching member, an `AttributeError` is
raised with a precise diagnostic pointing to the spec file, line number,
class name, variable name, and the fix needed.

The special variable `fields` is resolved via `self.fields()` if present,
allowing DataSchema subclasses to expose annotated field dictionaries.</docstring>
    <context>
      <feature_name>TemplateRendering</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Specification docstrings are treated as Jinja2 templates.

Undeclared variables in the combined base+instance template are collected
via `jinja2.meta.find_undeclared_variables`. Each variable name is mapped
to a same-named (with hyphens replaced by underscores) method or attribute
on the Ctx instance.

If a required variable has no matching member, an `AttributeError` is
raised with a precise diagnostic pointing to the spec file, line number,
class name, variable name, and the fix needed.

The special variable `fields` is resolved via `self.fields()` if present,
allowing DataSchema subclasses to expose annotated field dictionaries.</notes>
      <feature_name>TemplateRendering</feature_name>
    </delta_requirements>
  </specification>
  <specification type="TwoPassXmlAssembly" ref="spec.core.TwoPassXmlAssembly">
    <source target="TwoPassXmlAssembly" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>Specification XML is assembled in two passes to avoid eclipse bugs.

Pass 1 emits every full spec defined directly in the listed modules, in
the order they are discovered. This ensures that a class defined in the
project is never silently replaced by a thin dependency stub.

Pass 2 emits dependency stubs for inherited superspec classes that were
not already emitted in Pass 1. Stubs carry the superspec docstring
template and inheritance chain so that the diff engine can resolve
inherited requirements.

A ref-based deduplication set prevents any class from being emitted twice
across both passes.</docstring>
    <context>
      <feature_name>TwoPassXmlAssembly</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Specification XML is assembled in two passes to avoid eclipse bugs.

Pass 1 emits every full spec defined directly in the listed modules, in
the order they are discovered. This ensures that a class defined in the
project is never silently replaced by a thin dependency stub.

Pass 2 emits dependency stubs for inherited superspec classes that were
not already emitted in Pass 1. Stubs carry the superspec docstring
template and inheritance chain so that the diff engine can resolve
inherited requirements.

A ref-based deduplication set prevents any class from being emitted twice
across both passes.</notes>
      <feature_name>TwoPassXmlAssembly</feature_name>
    </delta_requirements>
  </specification>
  <specification type="XmlSerialization" ref="spec.core.XmlSerialization">
    <source target="XmlSerialization" file="/usr/backup-working/work/libspec/spec/core.py"/>
    <docstring>`to_xml_element()` builds the canonical &lt;specification&gt; XML element.

The element includes:
- type (class name) and ref (fully qualified name) attributes.
- &lt;source&gt; with target class name and absolute file path.
- &lt;docstring&gt; rendered from the Jinja2 template with the resolved context.
- &lt;context&gt; containing all resolved context fields as child elements.
- &lt;inherits&gt; listing fully qualified names of ancestor specs.
- &lt;effective_req_ids&gt; listing FQNs of all Requirement-derived ancestors.
- &lt;overrides&gt; listing field names that override parent values.
- &lt;delta_requirements&gt; containing fields that are new in this subclass.

Nested Python values (dicts, lists) are recursively serialized to XML
elements by `_to_xml_element()`. start_line and end_line keys are
omitted from context to avoid noisy diffs on line number changes.</docstring>
    <context>
      <feature_name>XmlSerialization</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`to_xml_element()` builds the canonical &lt;specification&gt; XML element.

The element includes:
- type (class name) and ref (fully qualified name) attributes.
- &lt;source&gt; with target class name and absolute file path.
- &lt;docstring&gt; rendered from the Jinja2 template with the resolved context.
- &lt;context&gt; containing all resolved context fields as child elements.
- &lt;inherits&gt; listing fully qualified names of ancestor specs.
- &lt;effective_req_ids&gt; listing FQNs of all Requirement-derived ancestors.
- &lt;overrides&gt; listing field names that override parent values.
- &lt;delta_requirements&gt; containing fields that are new in this subclass.

Nested Python values (dicts, lists) are recursively serialized to XML
elements by `_to_xml_element()`. start_line and end_line keys are
omitted from context to avoid noisy diffs on line number changes.</notes>
      <feature_name>XmlSerialization</feature_name>
    </delta_requirements>
  </specification>
  <specification type="BuildCommand" ref="spec.cli.BuildCommand">
    <source target="BuildCommand" file="/usr/backup-working/work/libspec/spec/cli.py"/>
    <docstring>`libspec build &lt;spec_file&gt; [-o &lt;output_dir&gt;]` generates the XML
specification from a Python spec file.

Module loading strategy:
- The spec file path is converted to a dotted module name relative to
  the current working directory (e.g. spec/main_spec.py -&gt; spec.main_spec).
- The CWD is prepended to sys.path so that relative imports within the
  spec package resolve correctly.
- The module is imported via importlib.import_module() so that
  __package__ is set correctly for relative imports (e.g. `from . import app`).

Discovery strategy:
- If the module contains an explicit Spec subclass, it is instantiated
  and `write_xml(output_dir)` is called directly.
- Otherwise, all Ctx subclasses in the module are discovered via
  `module_specs()` and wrapped in an ad-hoc Spec for output.</docstring>
    <context>
      <feature_name>BuildCommand</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`libspec build &lt;spec_file&gt; [-o &lt;output_dir&gt;]` generates the XML
specification from a Python spec file.

Module loading strategy:
- The spec file path is converted to a dotted module name relative to
  the current working directory (e.g. spec/main_spec.py -&gt; spec.main_spec).
- The CWD is prepended to sys.path so that relative imports within the
  spec package resolve correctly.
- The module is imported via importlib.import_module() so that
  __package__ is set correctly for relative imports (e.g. `from . import app`).

Discovery strategy:
- If the module contains an explicit Spec subclass, it is instantiated
  and `write_xml(output_dir)` is called directly.
- Otherwise, all Ctx subclasses in the module are discovered via
  `module_specs()` and wrapped in an ad-hoc Spec for output.</notes>
      <feature_name>BuildCommand</feature_name>
    </delta_requirements>
  </specification>
  <specification type="CLI" ref="spec.cli.CLI">
    <source target="CLI" file="/usr/backup-working/work/libspec/spec/cli.py"/>
    <docstring>The libspec command-line interface is implemented with docopt-ng.

The top-level usage string defines four subcommands:
  libspec init
  libspec build &lt;spec_file&gt; [-o &lt;output_dir&gt;]
  libspec diff &lt;build_dir&gt;
  libspec mcp

The --version flag reports the installed package version via
importlib.metadata. Help is available via -h / --help.

The default output directory for `build` is `spec-build`.</docstring>
    <context>
      <req_id>spec.cli.CLI</req_id>
      <title>CLI</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.cli.CLI</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The libspec command-line interface is implemented with docopt-ng.

The top-level usage string defines four subcommands:
  libspec init
  libspec build &lt;spec_file&gt; [-o &lt;output_dir&gt;]
  libspec diff &lt;build_dir&gt;
  libspec mcp

The --version flag reports the installed package version via
importlib.metadata. Help is available via -h / --help.

The default output directory for `build` is `spec-build`.</notes>
      <req_id>spec.cli.CLI</req_id>
      <title>CLI</title>
    </delta_requirements>
  </specification>
  <specification type="DiffCommand" ref="spec.cli.DiffCommand">
    <source target="DiffCommand" file="/usr/backup-working/work/libspec/spec/cli.py"/>
    <docstring>`libspec diff &lt;build_dir&gt;` diffs the two most recent XML spec files
in the given directory and prints a structured human-readable report.

The two files to diff are identified by the date-created attribute in
the XML root element (falling back to file mtime if absent). Files are
sorted by this timestamp and the two most recent are selected.

Bootstrap case: if only one XML file exists, a null spec (an empty
&lt;specification_set&gt;) is used as the "old" file so that the first build
produces a useful "everything is new" diff.

The diff refuses to run if the two files were generated by different
libspec major versions and prints a clear remediation message.</docstring>
    <context>
      <feature_name>DiffCommand</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`libspec diff &lt;build_dir&gt;` diffs the two most recent XML spec files
in the given directory and prints a structured human-readable report.

The two files to diff are identified by the date-created attribute in
the XML root element (falling back to file mtime if absent). Files are
sorted by this timestamp and the two most recent are selected.

Bootstrap case: if only one XML file exists, a null spec (an empty
&lt;specification_set&gt;) is used as the "old" file so that the first build
produces a useful "everything is new" diff.

The diff refuses to run if the two files were generated by different
libspec major versions and prints a clear remediation message.</notes>
      <feature_name>DiffCommand</feature_name>
    </delta_requirements>
  </specification>
  <specification type="InitCommand" ref="spec.cli.InitCommand">
    <source target="InitCommand" file="/usr/backup-working/work/libspec/spec/cli.py"/>
    <docstring>`libspec init` scaffolds a new spec/ directory in the current working
directory with three starter files:

- spec/__init__.py (empty)
- spec/main_spec.py (a MainSpec(Spec) class wired to the app module)
- spec/app.py (placeholder App and CmdLine classes)
- spec/err.py (Err base class + Feat/Req convenience aliases)

The command exits with an error if a spec/ directory already exists,
preventing accidental overwrite of an existing specification.</docstring>
    <context>
      <feature_name>InitCommand</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`libspec init` scaffolds a new spec/ directory in the current working
directory with three starter files:

- spec/__init__.py (empty)
- spec/main_spec.py (a MainSpec(Spec) class wired to the app module)
- spec/app.py (placeholder App and CmdLine classes)
- spec/err.py (Err base class + Feat/Req convenience aliases)

The command exits with an error if a spec/ directory already exists,
preventing accidental overwrite of an existing specification.</notes>
      <feature_name>InitCommand</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpCommand" ref="spec.cli.McpCommand">
    <source target="McpCommand" file="/usr/backup-working/work/libspec/spec/cli.py"/>
    <docstring>`libspec mcp` launches the MCP (Model Context Protocol) server over
stdio, making the MCP tools available to any MCP-capable LLM client:
- libspec_build
- libspec_diff

The MCP server is implemented with the FastMCP library and delegates
to the same underlying logic as the CLI subcommands.</docstring>
    <context>
      <feature_name>McpCommand</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`libspec mcp` launches the MCP (Model Context Protocol) server over
stdio, making the MCP tools available to any MCP-capable LLM client:
- libspec_build
- libspec_diff

The MCP server is implemented with the FastMCP library and delegates
to the same underlying logic as the CLI subcommands.</notes>
      <feature_name>McpCommand</feature_name>
    </delta_requirements>
  </specification>
  <specification type="DiffEngine" ref="spec.diff.DiffEngine">
    <source target="DiffEngine" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>`generate_patch(build_dir)` produces a structured diff between the two
most recent XML spec files in the given directory.

Output format:
- A header line: "Diffing State: &lt;old_file&gt; -&gt; &lt;new_file&gt;"
- A separator line of "=" characters.
- One block per changed/added/removed component, tagged [NEW], [REMOVED],
  or [CHANGED] followed by the component type name.
- Changed components list each field change as a bullet.
- A trailing [WARNING] block for any specs with unresolved inherited refs.

"No changes detected." is printed when there are no diffs and no
unresolved refs.</docstring>
    <context>
      <req_id>spec.diff.DiffEngine</req_id>
      <title>DiffEngine</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.diff.DiffEngine</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>`generate_patch(build_dir)` produces a structured diff between the two
most recent XML spec files in the given directory.

Output format:
- A header line: "Diffing State: &lt;old_file&gt; -&gt; &lt;new_file&gt;"
- A separator line of "=" characters.
- One block per changed/added/removed component, tagged [NEW], [REMOVED],
  or [CHANGED] followed by the component type name.
- Changed components list each field change as a bullet.
- A trailing [WARNING] block for any specs with unresolved inherited refs.

"No changes detected." is printed when there are no diffs and no
unresolved refs.</notes>
      <req_id>spec.diff.DiffEngine</req_id>
      <title>DiffEngine</title>
    </delta_requirements>
  </specification>
  <specification type="DocstringDiff" ref="spec.diff.DocstringDiff">
    <source target="DocstringDiff" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>Docstring changes are shown as unified diffs (--- old / +++ new).

Both &lt;docstring&gt; and &lt;docstring_template&gt; tags are checked; whichever
is present is used. The diff label is "docstring" for both.

The `_patch_block()` helper produces the unified diff block with
fromfile="old/&lt;label&gt;" and tofile="new/&lt;label&gt;" headers.</docstring>
    <context>
      <feature_name>DocstringDiff</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Docstring changes are shown as unified diffs (--- old / +++ new).

Both &lt;docstring&gt; and &lt;docstring_template&gt; tags are checked; whichever
is present is used. The diff label is "docstring" for both.

The `_patch_block()` helper produces the unified diff block with
fromfile="old/&lt;label&gt;" and tofile="new/&lt;label&gt;" headers.</notes>
      <feature_name>DocstringDiff</feature_name>
    </delta_requirements>
  </specification>
  <specification type="InheritanceDiff" ref="spec.diff.InheritanceDiff">
    <source target="InheritanceDiff" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>The Inherits field recursively diffs inherited superspecs.

When the set of inherited refs is identical between old and new, the
diff engine still checks whether the content of each common inherited
spec has changed by looking up the ref in old_specs_by_ref and
new_specs_by_ref and calling `_compare_specs()` recursively.

A `visited` set prevents infinite loops in circular inheritance graphs.

If a change is detected in an inherited spec, the message
"inherited spec '&lt;ref&gt;' changed" is appended to the component's changes.</docstring>
    <context>
      <feature_name>InheritanceDiff</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The Inherits field recursively diffs inherited superspecs.

When the set of inherited refs is identical between old and new, the
diff engine still checks whether the content of each common inherited
spec has changed by looking up the ref in old_specs_by_ref and
new_specs_by_ref and calling `_compare_specs()` recursively.

A `visited` set prevents infinite loops in circular inheritance graphs.

If a change is detected in an inherited spec, the message
"inherited spec '&lt;ref&gt;' changed" is appended to the component's changes.</notes>
      <feature_name>InheritanceDiff</feature_name>
    </delta_requirements>
  </specification>
  <specification type="InheritedContextDisplay" ref="spec.diff.InheritedContextDisplay">
    <source target="InheritedContextDisplay" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>For [CHANGED] and [NEW] components that have inherited refs, the
full text of each inherited superspec is printed under the heading
"inherited_specs (STRICTLY FOLLOW THE GUIDANCE BELOW):".

For template superspecs (template="true"), the docstring is rendered
through Jinja2 using the child component's context before display.

Refs that cannot be resolved in the current XML corpus are listed
under "unresolved_inherited_refs:" rather than silently dropped.</docstring>
    <context>
      <feature_name>InheritedContextDisplay</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>For [CHANGED] and [NEW] components that have inherited refs, the
full text of each inherited superspec is printed under the heading
"inherited_specs (STRICTLY FOLLOW THE GUIDANCE BELOW):".

For template superspecs (template="true"), the docstring is rendered
through Jinja2 using the child component's context before display.

Refs that cannot be resolved in the current XML corpus are listed
under "unresolved_inherited_refs:" rather than silently dropped.</notes>
      <feature_name>InheritedContextDisplay</feature_name>
    </delta_requirements>
  </specification>
  <specification type="NewSpecDisplay" ref="spec.diff.NewSpecDisplay">
    <source target="NewSpecDisplay" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>[NEW] components are printed using `_print_spec()` which calls
`display()` on each SpecField in order, passing a `seen_values` set to
prevent the same text from being printed twice (e.g. a docstring that
also appears verbatim as a delta_requirements note).

Components with no displayable content (no docstring, title, req_id,
description, notes, inherits, effective_req_ids, overrides, or non-
standard delta_requirements) are silently omitted from [NEW] output.</docstring>
    <context>
      <feature_name>NewSpecDisplay</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>[NEW] components are printed using `_print_spec()` which calls
`display()` on each SpecField in order, passing a `seen_values` set to
prevent the same text from being printed twice (e.g. a docstring that
also appears verbatim as a delta_requirements note).

Components with no displayable content (no docstring, title, req_id,
description, notes, inherits, effective_req_ids, overrides, or non-
standard delta_requirements) are silently omitted from [NEW] output.</notes>
      <feature_name>NewSpecDisplay</feature_name>
    </delta_requirements>
  </specification>
  <specification type="NullSpecDiff" ref="spec.diff.NullSpecDiff">
    <source target="NullSpecDiff" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>When only one XML file exists in the build directory, the diff runs
against a hard-coded NULL_SPEC_XML consisting of an empty
&lt;specification_set date-created="" /&gt; element.

This bootstrap case produces a [NEW] entry for every component in the
first build, giving a complete initial diff without requiring a second
build to be meaningful.

For display purposes the old file label is shown as "&lt;null spec&gt;".</docstring>
    <context>
      <feature_name>NullSpecDiff</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>When only one XML file exists in the build directory, the diff runs
against a hard-coded NULL_SPEC_XML consisting of an empty
&lt;specification_set date-created="" /&gt; element.

This bootstrap case produces a [NEW] entry for every component in the
first build, giving a complete initial diff without requiring a second
build to be meaningful.

For display purposes the old file label is shown as "&lt;null spec&gt;".</notes>
      <feature_name>NullSpecDiff</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SpecFieldPolymorphism" ref="spec.diff.SpecFieldPolymorphism">
    <source target="SpecFieldPolymorphism" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>Diff logic is organized as a hierarchy of SpecField classes, one per
logical field type. Each subclass implements:
- `display(seen_values)`: prints the field for the [NEW] case.
- `diff(old_spec)`: compares with old_spec and returns a change string
  or list of strings, or None if unchanged.

Field classes:
- Docstring: compares docstring / docstring_template text using unified diff.
- Title: compares the title context field.
- ReqId: compares the req_id context field.
- Description: compares the description context field.
- Notes: compares the notes context field.
- Inherits: compares the inherits ref set AND recursively diffs changed
  inherited specs to surface superspec mutations.
- EffectiveReqIds: compares the effective_req_ids id set.
- Overrides: compares the overrides field set.
- DeltaRequirements: compares all delta_requirements children not already
  handled by the primary field classes.</docstring>
    <context>
      <feature_name>SpecFieldPolymorphism</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Diff logic is organized as a hierarchy of SpecField classes, one per
logical field type. Each subclass implements:
- `display(seen_values)`: prints the field for the [NEW] case.
- `diff(old_spec)`: compares with old_spec and returns a change string
  or list of strings, or None if unchanged.

Field classes:
- Docstring: compares docstring / docstring_template text using unified diff.
- Title: compares the title context field.
- ReqId: compares the req_id context field.
- Description: compares the description context field.
- Notes: compares the notes context field.
- Inherits: compares the inherits ref set AND recursively diffs changed
  inherited specs to surface superspec mutations.
- EffectiveReqIds: compares the effective_req_ids id set.
- Overrides: compares the overrides field set.
- DeltaRequirements: compares all delta_requirements children not already
  handled by the primary field classes.</notes>
      <feature_name>SpecFieldPolymorphism</feature_name>
    </delta_requirements>
  </specification>
  <specification type="UnresolvedRefWarning" ref="spec.diff.UnresolvedRefWarning">
    <source target="UnresolvedRefWarning" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>After processing all component diffs, the engine checks for specs
that inherit refs not present in the current XML corpus.

If any are found, a [WARNING] block is printed listing each
(component, unresolved_ref) pair and advising the developer to
re-run the diff against a combined/merged XML to catch cross-file
superspec changes.

This warning exists because a child spec's XML may be byte-for-byte
identical even when its external superspec has changed, making silent
missed diffs possible without this check.</docstring>
    <context>
      <feature_name>UnresolvedRefWarning</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>After processing all component diffs, the engine checks for specs
that inherit refs not present in the current XML corpus.

If any are found, a [WARNING] block is printed listing each
(component, unresolved_ref) pair and advising the developer to
re-run the diff against a combined/merged XML to catch cross-file
superspec changes.

This warning exists because a child spec's XML may be byte-for-byte
identical even when its external superspec has changed, making silent
missed diffs possible without this check.</notes>
      <feature_name>UnresolvedRefWarning</feature_name>
    </delta_requirements>
  </specification>
  <specification type="VersionCompatibilityCheck" ref="spec.diff.VersionCompatibilityCheck">
    <source target="VersionCompatibilityCheck" file="/usr/backup-working/work/libspec/spec/diff.py"/>
    <docstring>Before running the diff, the major version of libspec that generated
each XML file is compared via the libspec-version root attribute.

If the major versions differ, the diff is aborted with an error message
explaining the mismatch and the remediation workflow
(rebuild both commits with the same libspec major version).

Missing version attributes are treated as compatible (None == None)
to support legacy XML files generated before versioning was added.</docstring>
    <context>
      <feature_name>VersionCompatibilityCheck</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Before running the diff, the major version of libspec that generated
each XML file is compared via the libspec-version root attribute.

If the major versions differ, the diff is aborted with an error message
explaining the mismatch and the remediation workflow
(rebuild both commits with the same libspec major version).

Missing version attributes are treated as compatible (None == None)
to support legacy XML files generated before versioning was added.</notes>
      <feature_name>VersionCompatibilityCheck</feature_name>
    </delta_requirements>
  </specification>
  <specification type="APIType" ref="spec.types.APIType">
    <source target="APIType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>API is a Ctx subclass (also mixing in LeafMethods) for specifying
class-level APIs.

Its docstring template enumerates all zero-argument and single-argument
public methods defined directly on the subclass (via LeafMethods), showing
each method name, parameter list, and its docstring description.

`api_name()` defaults to the class name. `constraints()` returns an
empty list by default; subclasses may override to add constraint strings.

LeafMethods inspects the leaf class `__dict__` (not inherited members) to
avoid duplicating inherited method listings.</docstring>
    <context>
      <feature_name>APIType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>API is a Ctx subclass (also mixing in LeafMethods) for specifying
class-level APIs.

Its docstring template enumerates all zero-argument and single-argument
public methods defined directly on the subclass (via LeafMethods), showing
each method name, parameter list, and its docstring description.

`api_name()` defaults to the class name. `constraints()` returns an
empty list by default; subclasses may override to add constraint strings.

LeafMethods inspects the leaf class `__dict__` (not inherited members) to
avoid duplicating inherited method listings.</notes>
      <feature_name>APIType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="BuiltInVocabulary" ref="spec.types.BuiltInVocabulary">
    <source target="BuiltInVocabulary" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>libspec ships a library of Ctx-derived base classes that projects can
use to structure their specifications. These are the canonical spec types
exported from `libspec` (via the top-level __init__.py).

Projects should import from this vocabulary and compose with multiple
inheritance rather than writing raw Ctx classes, so that the XML output
carries rich structured metadata for review and diff.</docstring>
    <context>
      <req_id>spec.types.BuiltInVocabulary</req_id>
      <title>BuiltInVocabulary</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.types.BuiltInVocabulary</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>libspec ships a library of Ctx-derived base classes that projects can
use to structure their specifications. These are the canonical spec types
exported from `libspec` (via the top-level __init__.py).

Projects should import from this vocabulary and compose with multiple
inheritance rather than writing raw Ctx classes, so that the XML output
carries rich structured metadata for review and diff.</notes>
      <req_id>spec.types.BuiltInVocabulary</req_id>
      <title>BuiltInVocabulary</title>
    </delta_requirements>
  </specification>
  <specification type="CmdLineType" ref="spec.types.CmdLineType">
    <source target="CmdLineType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>CmdLine is a Ctx subclass (also mixing in LeafMethods) for specifying
command-line interfaces.

Its docstring template enumerates all public methods on the leaf class,
showing the command name, parameter list, description, and a live preview
of the return value obtained by calling the method with None arguments.</docstring>
    <context>
      <feature_name>CmdLineType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>CmdLine is a Ctx subclass (also mixing in LeafMethods) for specifying
command-line interfaces.

Its docstring template enumerates all public methods on the leaf class,
showing the command name, parameter list, description, and a live preview
of the return value obtained by calling the method with None arguments.</notes>
      <feature_name>CmdLineType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="ConstraintType" ref="spec.types.ConstraintType">
    <source target="ConstraintType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>Constraint is a Ctx subclass for describing formal constraints.

Its docstring template renders three fields:
- `constraint_id`: defaults to the class name.
- `description`: defaults to the class docstring.
- `enforcement_logic`: must be provided by the subclass.</docstring>
    <context>
      <feature_name>ConstraintType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Constraint is a Ctx subclass for describing formal constraints.

Its docstring template renders three fields:
- `constraint_id`: defaults to the class name.
- `description`: defaults to the class docstring.
- `enforcement_logic`: must be provided by the subclass.</notes>
      <feature_name>ConstraintType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="DataSchemaType" ref="spec.types.DataSchemaType">
    <source target="DataSchemaType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>DataSchema is a Ctx subclass for describing data models.

Its docstring template renders `model_name` (defaults to the class name)
and a `fields` list derived from `__annotations__`. Subclasses define
fields using Python type annotations, which are rendered as a bullet list.</docstring>
    <context>
      <feature_name>DataSchemaType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>DataSchema is a Ctx subclass for describing data models.

Its docstring template renders `model_name` (defaults to the class name)
and a `fields` list derived from `__annotations__`. Subclasses define
fields using Python type annotations, which are rendered as a bullet list.</notes>
      <feature_name>DataSchemaType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="DefType" ref="spec.types.DefType">
    <source target="DefType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>Def is a Ctx subclass for capturing project-specific definitions
and glossary terms.

Its docstring template renders the field `name`, which returns the fully
qualified class name by default.</docstring>
    <context>
      <feature_name>DefType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Def is a Ctx subclass for capturing project-specific definitions
and glossary terms.

Its docstring template renders the field `name`, which returns the fully
qualified class name by default.</notes>
      <feature_name>DefType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="EdgeCaseType" ref="spec.types.EdgeCaseType">
    <source target="EdgeCaseType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>EdgeCase is a Ctx subclass for documenting boundary conditions and
exceptional scenarios.

Its docstring template renders two fields: `boundary_condition` and
`error_scenario`. Both raise UnimplementedMethodError by default and
must be overridden by subclasses.</docstring>
    <context>
      <feature_name>EdgeCaseType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>EdgeCase is a Ctx subclass for documenting boundary conditions and
exceptional scenarios.

Its docstring template renders two fields: `boundary_condition` and
`error_scenario`. Both raise UnimplementedMethodError by default and
must be overridden by subclasses.</notes>
      <feature_name>EdgeCaseType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="FeatureType" ref="spec.types.FeatureType">
    <source target="FeatureType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>Feature is a Ctx subclass for describing named product features.

Its docstring template renders the field `feature_name`, which defaults
to the subclass class name. Projects may override `feature_name()` to
return a custom display name.

Subclasses must also implement `date()` and `description()` if those
fields appear in the template; they raise UnimplementedMethodError by
default to force explicit overrides.</docstring>
    <context>
      <feature_name>FeatureType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Feature is a Ctx subclass for describing named product features.

Its docstring template renders the field `feature_name`, which defaults
to the subclass class name. Projects may override `feature_name()` to
return a custom display name.

Subclasses must also implement `date()` and `description()` if those
fields appear in the template; they raise UnimplementedMethodError by
default to force explicit overrides.</notes>
      <feature_name>FeatureType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="ImplementationType" ref="spec.types.ImplementationType">
    <source target="ImplementationType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>Implementation extends Requirement with guidance that implementations
must include tests and specifies the target directory.

The template field `implementation_directory` must be provided by the
subclass to indicate where generated files should live.</docstring>
    <context>
      <feature_name>ImplementationType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Implementation extends Requirement with guidance that implementations
must include tests and specifies the target directory.

The template field `implementation_directory` must be provided by the
subclass to indicate where generated files should live.</notes>
      <feature_name>ImplementationType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="LeafMethodsMixin" ref="spec.types.LeafMethodsMixin">
    <source target="LeafMethodsMixin" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>LeafMethods is a plain Python mixin (not Ctx-derived) that provides
method introspection for API and CmdLine spec classes.

`methods()` returns a list of descriptor dicts for every public function
defined directly in the leaf class `__dict__` (not inherited members),
preventing duplicate listings of inherited methods.

Each descriptor contains:
- `name`: the method name.
- `params`: parameter names excluding "self".
- `description`: the cleaned docstring of the function.
- `result`: a live preview obtained by calling the method with None
  arguments (for zero- and single-argument methods).
- `source_ref`: source file and line range from `_source_info()`.

API and CmdLine both inherit from both Ctx and LeafMethods via multiple
inheritance. The `methods` attribute is consumed by their Jinja2 docstring
templates to enumerate endpoints or commands.</docstring>
    <context>
      <feature_name>LeafMethodsMixin</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>LeafMethods is a plain Python mixin (not Ctx-derived) that provides
method introspection for API and CmdLine spec classes.

`methods()` returns a list of descriptor dicts for every public function
defined directly in the leaf class `__dict__` (not inherited members),
preventing duplicate listings of inherited methods.

Each descriptor contains:
- `name`: the method name.
- `params`: parameter names excluding "self".
- `description`: the cleaned docstring of the function.
- `result`: a live preview obtained by calling the method with None
  arguments (for zero- and single-argument methods).
- `source_ref`: source file and line range from `_source_info()`.

API and CmdLine both inherit from both Ctx and LeafMethods via multiple
inheritance. The `methods` attribute is consumed by their Jinja2 docstring
templates to enumerate endpoints or commands.</notes>
      <feature_name>LeafMethodsMixin</feature_name>
    </delta_requirements>
  </specification>
  <specification type="LibraryAPIType" ref="spec.types.LibraryAPIType">
    <source target="LibraryAPIType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>LibraryAPI extends API with a version display and the clarification
that this is a library API, not a network API.

Subclasses must implement `version()`.</docstring>
    <context>
      <feature_name>LibraryAPIType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>LibraryAPI extends API with a version display and the clarification
that this is a library API, not a network API.

Subclasses must implement `version()`.</notes>
      <feature_name>LibraryAPIType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="PeeWeeType" ref="spec.types.PeeWeeType">
    <source target="PeeWeeType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>PeeWee extends DataSchema with guidance to implement the schema
using the PeeWee ORM and write tests verifying database behavior.

Adds the template field `dbpath` requiring subclasses to specify
the database file location.</docstring>
    <context>
      <feature_name>PeeWeeType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>PeeWee extends DataSchema with guidance to implement the schema
using the PeeWee ORM and write tests verifying database behavior.

Adds the template field `dbpath` requiring subclasses to specify
the database file location.</notes>
      <feature_name>PeeWeeType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="RequirementType" ref="spec.types.RequirementType">
    <source target="RequirementType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>Requirement is a Ctx subclass for describing formal requirements.

Its docstring template renders two fields: `title` (defaults to the class
name) and `req_id` (defaults to the fully qualified class name). The
req_id is intended to be embedded as a comment in generated source code so
that downstream tooling can trace generated code back to the spec source.</docstring>
    <context>
      <feature_name>RequirementType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Requirement is a Ctx subclass for describing formal requirements.

Its docstring template renders two fields: `title` (defaults to the class
name) and `req_id` (defaults to the fully qualified class name). The
req_id is intended to be embedded as a comment in generated source code so
that downstream tooling can trace generated code back to the spec source.</notes>
      <feature_name>RequirementType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="RestMixinType" ref="spec.types.RestMixinType">
    <source target="RestMixinType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>RestMixin is a plain Ctx mixin carrying the guidance to develop a REST
API with best practices around the interface.

Mix RestMixin into an API subclass to layer REST-specific guidance onto
an existing API specification.</docstring>
    <context>
      <feature_name>RestMixinType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>RestMixin is a plain Ctx mixin carrying the guidance to develop a REST
API with best practices around the interface.

Mix RestMixin into an API subclass to layer REST-specific guidance onto
an existing API specification.</notes>
      <feature_name>RestMixinType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SQLite3Type" ref="spec.types.SQLite3Type">
    <source target="SQLite3Type" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>SQLite3 extends DataSchema with guidance to implement the schema
using SQLite3 and write tests verifying database behavior.

Adds the template field `dbpath` requiring subclasses to specify
the database file location.</docstring>
    <context>
      <feature_name>SQLite3Type</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>SQLite3 extends DataSchema with guidance to implement the schema
using SQLite3 and write tests verifying database behavior.

Adds the template field `dbpath` requiring subclasses to specify
the database file location.</notes>
      <feature_name>SQLite3Type</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SystemRequirementType" ref="spec.types.SystemRequirementType">
    <source target="SystemRequirementType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>SystemRequirement extends Requirement with an additional note that the
requirement is aimed at the toolchain supporting the project rather than
product behavior.

Use SystemRequirement for specs that constrain the build system, CI
pipeline, packaging, or other infrastructure.</docstring>
    <context>
      <feature_name>SystemRequirementType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>SystemRequirement extends Requirement with an additional note that the
requirement is aimed at the toolchain supporting the project rather than
product behavior.

Use SystemRequirement for specs that constrain the build system, CI
pipeline, packaging, or other infrastructure.</notes>
      <feature_name>SystemRequirementType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="UserStoryType" ref="spec.types.UserStoryType">
    <source target="UserStoryType" file="/usr/backup-working/work/libspec/spec/types.py"/>
    <docstring>UserStory extends Feature with a structured user story template.

The template uses single-brace placeholder syntax (not Jinja2 double-brace)
as a documentation convention. Fields like brief-title, priority,
user-journey, explanation, and acceptance scenarios are filled in by the
spec author as free text in the subclass docstring rather than resolved
from Python methods.</docstring>
    <context>
      <feature_name>UserStoryType</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>UserStory extends Feature with a structured user story template.

The template uses single-brace placeholder syntax (not Jinja2 double-brace)
as a documentation convention. Fields like brief-title, priority,
user-journey, explanation, and acceptance scenarios are filled in by the
spec author as free text in the subclass docstring rather than resolved
from Python methods.</notes>
      <feature_name>UserStoryType</feature_name>
    </delta_requirements>
  </specification>
  <specification type="AgentConfig" ref="spec.mcp.AgentConfig">
    <source target="AgentConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Base requirement for project-local agent configuration.

All agents must be configured to use `uv run libspec mcp` to
ensure they utilize the project's local environment.

Backups:
To prevent data loss during configuration updates, the system must 
create a backup of any existing configuration file before performing 
a mutation. The backup should:
1. Only be created if the configuration file already exists.
2. Use the naming convention `&lt;original_filename&gt;.bak`.
3. Be overwritten on subsequent updates (only the immediate previous 
   state is preserved).</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.AgentConfig</req_id>
      <title>AgentConfig</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Base requirement for project-local agent configuration.

All agents must be configured to use `uv run libspec mcp` to
ensure they utilize the project's local environment.

Backups:
To prevent data loss during configuration updates, the system must 
create a backup of any existing configuration file before performing 
a mutation. The backup should:
1. Only be created if the configuration file already exists.
2. Use the naming convention `&lt;original_filename&gt;.bak`.
3. Be overwritten on subsequent updates (only the immediate previous 
   state is preserved).</notes>
      <req_id>spec.mcp.AgentConfig</req_id>
      <title>AgentConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="AgentSkillInstallation" ref="spec.mcp.AgentSkillInstallation">
    <source target="AgentSkillInstallation" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>During agent configuration, the libspec skill must be installed 
using the **SkillKit** Python library to ensure structural and 
frontmatter compliance.

The installation process must:
1. Utilize `skillkit.core.parser.SkillParser` to validate the skill content 
   and frontmatter before any disk operation.
2. Ensure the structural layout and filenames (`SKILL.md`) strictly 
   comply with SkillKit's discovery standards.
3. Programmatically manage the skill placement to ensure proper 
   discovery across all supported agents (Antigravity, Gemini, OpenCode, 
   Copilot, Codex, Claude).
4. Utilize a centralized **Jinja2 template** for generating skill content 
   to ensure detailed and consistent documentation of all Libspec tools 
   (`search`, `peek`, `symbols`, `usage`).</docstring>
    <context>
      <feature_name>AgentSkillInstallation</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>During agent configuration, the libspec skill must be installed 
using the **SkillKit** Python library to ensure structural and 
frontmatter compliance.

The installation process must:
1. Utilize `skillkit.core.parser.SkillParser` to validate the skill content 
   and frontmatter before any disk operation.
2. Ensure the structural layout and filenames (`SKILL.md`) strictly 
   comply with SkillKit's discovery standards.
3. Programmatically manage the skill placement to ensure proper 
   discovery across all supported agents (Antigravity, Gemini, OpenCode, 
   Copilot, Codex, Claude).
4. Utilize a centralized **Jinja2 template** for generating skill content 
   to ensure detailed and consistent documentation of all Libspec tools 
   (`search`, `peek`, `symbols`, `usage`).</notes>
      <feature_name>AgentSkillInstallation</feature_name>
    </delta_requirements>
  </specification>
  <specification type="AntigravityConfig" ref="spec.mcp.AntigravityConfig">
    <source target="AntigravityConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Antigravity configuration requirement.
The registration must be written to `.gemini/antigravity/mcp_config.json` in the
project root to enable seamless project-local discovery.</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.AntigravityConfig</req_id>
      <title>AntigravityConfig</title>
    </context>
    <inherits>
      <ref>spec.mcp.AgentConfig</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.AntigravityConfig</id>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Antigravity configuration requirement.
The registration must be written to `.gemini/antigravity/mcp_config.json` in the
project root to enable seamless project-local discovery.</notes>
      <req_id>spec.mcp.AntigravityConfig</req_id>
      <title>AntigravityConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="ClaudeConfig" ref="spec.mcp.ClaudeConfig">
    <source target="ClaudeConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Claude Desktop configuration requirement.
Since Claude primarily uses a global config, the tool should provide
the exact JSON snippet for the user to append to their
`claude_desktop_config.json`.</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.ClaudeConfig</req_id>
      <title>ClaudeConfig</title>
    </context>
    <inherits>
      <ref>spec.mcp.AgentConfig</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.ClaudeConfig</id>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Claude Desktop configuration requirement.
Since Claude primarily uses a global config, the tool should provide
the exact JSON snippet for the user to append to their
`claude_desktop_config.json`.</notes>
      <req_id>spec.mcp.ClaudeConfig</req_id>
      <title>ClaudeConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="CodexConfig" ref="spec.mcp.CodexConfig">
    <source target="CodexConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Codex configuration requirement.

Codex must be able to discover and launch the libspec MCP server from the 
project workspace using the correct Codex configuration format.

The project must load MCP configuration from `.codex/config.toml` in the 
project root, or from `~/.codex/config.toml` when the configuration 
is user-scoped.

The MCP server must be declared as a TOML table named `mcp_servers.libspec`.

The configuration must contain:
```toml
[mcp_servers.libspec]
command = "uv"
args = ["run", "libspec", "mcp"]
cwd = "&lt;project-root&gt;"
```

Behavior:
1. Create `.codex/` if it does not already exist.
2. Read any existing `.codex/config.toml` file.
3. Preserve unrelated settings already present in that file.
4. Add or replace only the `mcp_servers.libspec` entry.
5. Write valid TOML, not JSON.
6. Use `uv run libspec mcp` with the repository root as `cwd`.
7. Install a dedicated `libspec.md` skill into `.codex/` to guide 
   Codex behavior.</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.CodexConfig</req_id>
      <title>CodexConfig</title>
    </context>
    <inherits>
      <ref>spec.mcp.AgentConfig</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.CodexConfig</id>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Codex configuration requirement.

Codex must be able to discover and launch the libspec MCP server from the 
project workspace using the correct Codex configuration format.

The project must load MCP configuration from `.codex/config.toml` in the 
project root, or from `~/.codex/config.toml` when the configuration 
is user-scoped.

The MCP server must be declared as a TOML table named `mcp_servers.libspec`.

The configuration must contain:
```toml
[mcp_servers.libspec]
command = "uv"
args = ["run", "libspec", "mcp"]
cwd = "&lt;project-root&gt;"
```

Behavior:
1. Create `.codex/` if it does not already exist.
2. Read any existing `.codex/config.toml` file.
3. Preserve unrelated settings already present in that file.
4. Add or replace only the `mcp_servers.libspec` entry.
5. Write valid TOML, not JSON.
6. Use `uv run libspec mcp` with the repository root as `cwd`.
7. Install a dedicated `libspec.md` skill into `.codex/` to guide 
   Codex behavior.</notes>
      <req_id>spec.mcp.CodexConfig</req_id>
      <title>CodexConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="CopilotConfig" ref="spec.mcp.CopilotConfig">
    <source target="CopilotConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>GitHub Copilot configuration requirement.
The registration must be written to `.copilot/mcp.json` in the
project root.</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.CopilotConfig</req_id>
      <title>CopilotConfig</title>
    </context>
    <inherits>
      <ref>spec.mcp.AgentConfig</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.CopilotConfig</id>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>GitHub Copilot configuration requirement.
The registration must be written to `.copilot/mcp.json` in the
project root.</notes>
      <req_id>spec.mcp.CopilotConfig</req_id>
      <title>CopilotConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="LspTool" ref="spec.mcp.LspTool">
    <source target="LspTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Ensure the background LSP process is initialized before
execution.  If the LSP is not running, the tool should trigger an
automatic, transparent start-up sequence.</docstring>
    <context>
      <feature_name>LspTool</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Ensure the background LSP process is initialized before
execution.  If the LSP is not running, the tool should trigger an
automatic, transparent start-up sequence.</notes>
      <feature_name>LspTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpAgentList" ref="spec.mcp.McpAgentList">
    <source target="McpAgentList" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The agent configuration tool must support listing all available 
agent configuration strategies.

This allows users to discover supported agents (e.g., "antigravity", 
"copilot", "codex") without referring to external documentation.

The list must be:
1. Alphabetically sorted.
2. Formatted for easy CLI reading.</docstring>
    <context>
      <feature_name>McpAgentList</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The agent configuration tool must support listing all available 
agent configuration strategies.

This allows users to discover supported agents (e.g., "antigravity", 
"copilot", "codex") without referring to external documentation.

The list must be:
1. Alphabetically sorted.
2. Formatted for easy CLI reading.</notes>
      <feature_name>McpAgentList</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpAutoDiscover" ref="spec.mcp.McpAutoDiscover">
    <source target="McpAutoDiscover" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The libspec MCP server must support automatic discovery of its
environment to ensure a zero-config experience.

When initialized, the server should detect:
1. The presence of a `spec/` directory.
2. The project's local python environment (via uv/venv).
3. The appropriate LSP configuration based on the project structure.

This ensures that tools like `libspec_start_lsp` work out-of-the-box
without requiring manual path configurations.</docstring>
    <context>
      <req_id>spec.mcp.McpAutoDiscover</req_id>
      <title>McpAutoDiscover</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.McpAutoDiscover</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The libspec MCP server must support automatic discovery of its
environment to ensure a zero-config experience.

When initialized, the server should detect:
1. The presence of a `spec/` directory.
2. The project's local python environment (via uv/venv).
3. The appropriate LSP configuration based on the project structure.

This ensures that tools like `libspec_start_lsp` work out-of-the-box
without requiring manual path configurations.</notes>
      <req_id>spec.mcp.McpAutoDiscover</req_id>
      <title>McpAutoDiscover</title>
    </delta_requirements>
  </specification>
  <specification type="McpBuildTool" ref="spec.mcp.McpBuildTool">
    <source target="McpBuildTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_build` MCP tool builds an XML spec from
a Python spec file.

Parameters:
- spec_file (str, optional): Path to the main Python spec file. If
  omitted, the tool auto-discovers candidates by globbing for *_spec.py,
  spec.py, and spec/*_spec.py in the current working directory.
- output_dir (str, default "spec-build"): Output directory.

The tool delegates to `libspec build` via subprocess and returns the
combined stdout or a formatted error message on failure.</docstring>
    <context>
      <feature_name>McpBuildTool</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_build` MCP tool builds an XML spec from
a Python spec file.

Parameters:
- spec_file (str, optional): Path to the main Python spec file. If
  omitted, the tool auto-discovers candidates by globbing for *_spec.py,
  spec.py, and spec/*_spec.py in the current working directory.
- output_dir (str, default "spec-build"): Output directory.

The tool delegates to `libspec build` via subprocess and returns the
combined stdout or a formatted error message on failure.</notes>
      <feature_name>McpBuildTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpConfigTool" ref="spec.mcp.McpConfigTool">
    <source target="McpConfigTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_mcp_config` MCP tool enables project-local registration
of the libspec MCP server.

It detects the project environment and updates the specified agent's
local configuration to include the `libspec` MCP server using
`uv run libspec mcp` as the execution command.

Parameters:
- agent (str): The name of the coding agent (e.g., "antigravity", "claude", "vscode").
- project_root (str, default "."): The root directory of the project.

Returns a success message with the path to the updated config file.
If `list_agents` is True, returns a formatted list of all supported 
agent names instead.</docstring>
    <context>
      <feature_name>McpConfigTool</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_mcp_config` MCP tool enables project-local registration
of the libspec MCP server.

It detects the project environment and updates the specified agent's
local configuration to include the `libspec` MCP server using
`uv run libspec mcp` as the execution command.

Parameters:
- agent (str): The name of the coding agent (e.g., "antigravity", "claude", "vscode").
- project_root (str, default "."): The root directory of the project.

Returns a success message with the path to the updated config file.
If `list_agents` is True, returns a formatted list of all supported 
agent names instead.</notes>
      <feature_name>McpConfigTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpDiffTool" ref="spec.mcp.McpDiffTool">
    <source target="McpDiffTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_diff` MCP tool diffs the two latest XML specs in a
build directory.

Parameters:
- build_dir (str, default "spec-build"): Directory containing XML files.

The tool delegates to `libspec diff` via subprocess. Returns the diff
output or "No changes detected." on success, or a formatted error
message on failure.</docstring>
    <context>
      <feature_name>McpDiffTool</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_diff` MCP tool diffs the two latest XML specs in a
build directory.

Parameters:
- build_dir (str, default "spec-build"): Directory containing XML files.

The tool delegates to `libspec diff` via subprocess. Returns the diff
output or "No changes detected." on success, or a formatted error
message on failure.</notes>
      <feature_name>McpDiffTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpPeekTool" ref="spec.mcp.McpPeekTool">
    <source target="McpPeekTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_peek` tool provides immediate context and
location for a component at a specific position.

Parameters:
- file_path (str): Path to the file.
- line (int): 0-indexed line number.
- character (int): 0-indexed character offset.

Returns a combined response containing:
1. The rendered docstring/requirement text.
2. The type information.
3. The file path and line number of the definition.</docstring>
    <context>
      <feature_name>McpPeekTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_peek` tool provides immediate context and
location for a component at a specific position.

Parameters:
- file_path (str): Path to the file.
- line (int): 0-indexed line number.
- character (int): 0-indexed character offset.

Returns a combined response containing:
1. The rendered docstring/requirement text.
2. The type information.
3. The file path and line number of the definition.</notes>
      <feature_name>McpPeekTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpPylspPluginTool" ref="spec.mcp.McpPylspPluginTool">
    <source target="McpPylspPluginTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_pylsp_plugin` tool allows enabling or disabling 
pylsp plugins dynamically.

Parameters:
- plugin_name (str): The name of the plugin (e.g., "hello", "pyflakes").
- action (str, default "status"): "status", "enable", or "disable".</docstring>
    <context>
      <feature_name>McpPylspPluginTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_pylsp_plugin` tool allows enabling or disabling 
pylsp plugins dynamically.

Parameters:
- plugin_name (str): The name of the plugin (e.g., "hello", "pyflakes").
- action (str, default "status"): "status", "enable", or "disable".</notes>
      <feature_name>McpPylspPluginTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpSearchTool" ref="spec.mcp.McpSearchTool">
    <source target="McpSearchTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_search` tool is the primary discovery tool for
the agent. It performs a workspace-wide semantic search for components
by name.

Parameters:
- query (str): The name of the component (class, method, or variable).

Returns a list of semantic matches with their file paths and line
numbers, allowing the agent to jump directly to definitions without
using grep.</docstring>
    <context>
      <feature_name>McpSearchTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_search` tool is the primary discovery tool for
the agent. It performs a workspace-wide semantic search for components
by name.

Parameters:
- query (str): The name of the component (class, method, or variable).

Returns a list of semantic matches with their file paths and line
numbers, allowing the agent to jump directly to definitions without
using grep.</notes>
      <feature_name>McpSearchTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpServer" ref="spec.mcp.McpServer">
    <source target="McpServer" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The libspec MCP server exposes tools over the stdio transport
using the FastMCP library, making libspec capabilities available to any
MCP-compatible LLM client (e.g. Claude Desktop, opencode).

The server is launched via `libspec mcp` or `libspec-mcp` (both are
registered as entry points in pyproject.toml).

Tools are stateless wrappers around the same logic as the CLI subcommands.

Instructions:
The server must provide global usage instructions (Server Instructions) 
to the LLM during initialization to guide its behavior.</docstring>
    <context>
      <req_id>spec.mcp.McpServer</req_id>
      <title>McpServer</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.McpServer</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The libspec MCP server exposes tools over the stdio transport
using the FastMCP library, making libspec capabilities available to any
MCP-compatible LLM client (e.g. Claude Desktop, opencode).

The server is launched via `libspec mcp` or `libspec-mcp` (both are
registered as entry points in pyproject.toml).

Tools are stateless wrappers around the same logic as the CLI subcommands.

Instructions:
The server must provide global usage instructions (Server Instructions) 
to the LLM during initialization to guide its behavior.</notes>
      <req_id>spec.mcp.McpServer</req_id>
      <title>McpServer</title>
    </delta_requirements>
  </specification>
  <specification type="McpServerInstructions" ref="spec.mcp.McpServerInstructions">
    <source target="McpServerInstructions" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>Global guidance provided to the LLM via the MCP `instructions` capability.

The instructions must:
1. Guide the LLM to prefer LSP-based tools (`search`, `peek`, `symbols`, `usage`) 
   over generic `grep` when analyzing the codebase for semantic understanding.
2. Explain that `search` combines native spec discovery with LSP 
   symbol search.
3. Instruct the LLM to use `peek` for definitions and hover info 
   instead of reading full files when looking for specific component logic.
4. Mention that the server auto-initializes the background LSP 
   process on the first relevant tool call.</docstring>
    <context>
      <feature_name>McpServerInstructions</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Global guidance provided to the LLM via the MCP `instructions` capability.

The instructions must:
1. Guide the LLM to prefer LSP-based tools (`search`, `peek`, `symbols`, `usage`) 
   over generic `grep` when analyzing the codebase for semantic understanding.
2. Explain that `search` combines native spec discovery with LSP 
   symbol search.
3. Instruct the LLM to use `peek` for definitions and hover info 
   instead of reading full files when looking for specific component logic.
4. Mention that the server auto-initializes the background LSP 
   process on the first relevant tool call.</notes>
      <feature_name>McpServerInstructions</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpSetPylspSettingTool" ref="spec.mcp.McpSetPylspSettingTool">
    <source target="McpSetPylspSettingTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_set_pylsp_plugin_setting` tool allows dynamic 
tuning of plugin parameters over LSP.

Parameters:
- plugin_name (str): The name of the plugin (e.g., "hello_ast").
- setting_name (str): The specific setting key (e.g., "pattern").
- value (str): The value to apply (JSON-parsed if possible).</docstring>
    <context>
      <feature_name>McpSetPylspSettingTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_set_pylsp_plugin_setting` tool allows dynamic 
tuning of plugin parameters over LSP.

Parameters:
- plugin_name (str): The name of the plugin (e.g., "hello_ast").
- setting_name (str): The specific setting key (e.g., "pattern").
- value (str): The value to apply (JSON-parsed if possible).</notes>
      <feature_name>McpSetPylspSettingTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpStartLspTool" ref="spec.mcp.McpStartLspTool">
    <source target="McpStartLspTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_start_lsp` MCP tool launches a Language Server
specialized for specification-driven development.

The LSP server is built on `pylsp` and uses a custom `libspec` plugin.
Crucially, this tool implements a delegation model: while `pylsp`
handles AST and standard Python features, the `libspec` plugin
delegates semantic requirement validation and docstring rendering
"up" to the active coding agent.

Parameters:
- root_dir (str, default "spec"): The root directory of the specification.</docstring>
    <context>
      <feature_name>McpStartLspTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_start_lsp` MCP tool launches a Language Server
specialized for specification-driven development.

The LSP server is built on `pylsp` and uses a custom `libspec` plugin.
Crucially, this tool implements a delegation model: while `pylsp`
handles AST and standard Python features, the `libspec` plugin
delegates semantic requirement validation and docstring rendering
"up" to the active coding agent.

Parameters:
- root_dir (str, default "spec"): The root directory of the specification.</notes>
      <feature_name>McpStartLspTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpSymbolsTool" ref="spec.mcp.McpSymbolsTool">
    <source target="McpSymbolsTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_symbols` tool provides a structural overview of
a file's contents.

Parameters:
- file_path (str): Path to the file.

Returns a hierarchical list of components (classes, methods) to help
the agent orient itself within a new file.</docstring>
    <context>
      <feature_name>McpSymbolsTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_symbols` tool provides a structural overview of
a file's contents.

Parameters:
- file_path (str): Path to the file.

Returns a hierarchical list of components (classes, methods) to help
the agent orient itself within a new file.</notes>
      <feature_name>McpSymbolsTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="McpUsageTool" ref="spec.mcp.McpUsageTool">
    <source target="McpUsageTool" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>The `libspec_usage` tool finds all semantic references to a
component, allowing the agent to understand how it is used.

Parameters:
- file_path (str): Path to the file.
- line (int): 0-indexed line number.
- character (int): 0-indexed character offset.

Returns a list of usage locations, optimized for navigation.</docstring>
    <context>
      <feature_name>McpUsageTool</feature_name>
    </context>
    <inherits>
      <ref>spec.mcp.LspTool</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_usage` tool finds all semantic references to a
component, allowing the agent to understand how it is used.

Parameters:
- file_path (str): Path to the file.
- line (int): 0-indexed line number.
- character (int): 0-indexed character offset.

Returns a list of usage locations, optimized for navigation.</notes>
      <feature_name>McpUsageTool</feature_name>
    </delta_requirements>
  </specification>
  <specification type="OpenCodeConfig" ref="spec.mcp.OpenCodeConfig">
    <source target="OpenCodeConfig" file="/usr/backup-working/work/libspec/spec/mcp.py"/>
    <docstring>OpenCode configuration requirement.
OpenCode registration must be written to `.opencode/opencode.json`
in the project root.</docstring>
    <context>
      <configure>None</configure>
      <req_id>spec.mcp.OpenCodeConfig</req_id>
      <title>OpenCodeConfig</title>
    </context>
    <inherits>
      <ref>spec.mcp.AgentConfig</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.mcp.OpenCodeConfig</id>
      <id>spec.mcp.AgentConfig</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>OpenCode configuration requirement.
OpenCode registration must be written to `.opencode/opencode.json`
in the project root.</notes>
      <req_id>spec.mcp.OpenCodeConfig</req_id>
      <title>OpenCodeConfig</title>
      <configure>None</configure>
    </delta_requirements>
  </specification>
  <specification type="SpecDiscovery" ref="spec.utils.SpecDiscovery">
    <source target="SpecDiscovery" file="/usr/backup-working/work/libspec/spec/utils.py"/>
    <docstring>Module-level functions that discover and instantiate Ctx-derived classes.

`ctx_spec_classes_in_module(module)` scans a module's members and returns
all classes whose `__module__` attribute matches the module being inspected.
This prevents re-emitting imported base classes like Ctx itself.

`instantiate_module_specs(module)` calls `ctx_spec_classes_in_module` and
returns a list of zero-argument instances, one per discovered class.

`module_specs(mod)` is a legacy alias for `instantiate_module_specs`.
`classes_with_ctx_superclass(module)` is a legacy alias for
`ctx_spec_classes_in_module`.

The `_MissingType` / `_Missing` sentinel is a private singleton used
internally by Ctx to distinguish "no value returned" from `None`.</docstring>
    <context>
      <feature_name>SpecDiscovery</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Module-level functions that discover and instantiate Ctx-derived classes.

`ctx_spec_classes_in_module(module)` scans a module's members and returns
all classes whose `__module__` attribute matches the module being inspected.
This prevents re-emitting imported base classes like Ctx itself.

`instantiate_module_specs(module)` calls `ctx_spec_classes_in_module` and
returns a list of zero-argument instances, one per discovered class.

`module_specs(mod)` is a legacy alias for `instantiate_module_specs`.
`classes_with_ctx_superclass(module)` is a legacy alias for
`ctx_spec_classes_in_module`.

The `_MissingType` / `_Missing` sentinel is a private singleton used
internally by Ctx to distinguish "no value returned" from `None`.</notes>
      <feature_name>SpecDiscovery</feature_name>
    </delta_requirements>
  </specification>
  <specification type="UnimplementedMethod" ref="spec.utils.UnimplementedMethod">
    <source target="UnimplementedMethod" file="/usr/backup-working/work/libspec/spec/utils.py"/>
    <docstring>UnimplementedMethodError is a NotImplementedError subclass raised by
abstract methods in Ctx-derived spec classes that have no default behavior.

On construction it uses Python frame introspection to automatically include
the calling method name and the instance class name in the error message,
so callers get a precise diagnostic without any boilerplate in the raiser:

    def my_method(self):
        raise UnimplementedMethodError()
    # -&gt; "Method 'my_method' is not implemented in class 'MyClass'"

An optional `message` argument appends extra context to the auto-generated
text. The class lives in `libspec.err` and is the only custom exception
exported from the library.</docstring>
    <context>
      <req_id>spec.utils.UnimplementedMethod</req_id>
      <title>UnimplementedMethod</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.utils.UnimplementedMethod</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>UnimplementedMethodError is a NotImplementedError subclass raised by
abstract methods in Ctx-derived spec classes that have no default behavior.

On construction it uses Python frame introspection to automatically include
the calling method name and the instance class name in the error message,
so callers get a precise diagnostic without any boilerplate in the raiser:

    def my_method(self):
        raise UnimplementedMethodError()
    # -&gt; "Method 'my_method' is not implemented in class 'MyClass'"

An optional `message` argument appends extra context to the auto-generated
text. The class lives in `libspec.err` and is the only custom exception
exported from the library.</notes>
      <req_id>spec.utils.UnimplementedMethod</req_id>
      <title>UnimplementedMethod</title>
    </delta_requirements>
  </specification>
  <specification type="UtilityFunctions" ref="spec.utils.UtilityFunctions">
    <source target="UtilityFunctions" file="/usr/backup-working/work/libspec/spec/utils.py"/>
    <docstring>`libspec.util` provides a handful of pure utility helpers used
internally across the library.

- `fqn(obj)`: Returns the fully qualified name of a class or instance
  as "module.qualname", used as the canonical XML ref attribute.

- `easy_hash(text)`: Returns the MD5 hex digest of a UTF-8 string,
  used to compute the content-addressed XML filename.

- `get_libspec_version()`: Returns the installed package version via
  `importlib.metadata`, falling back to "unknown" if unavailable.

- `diff_two_latest(dirpath)`: (Legacy) Returns a unified diff of the
  two most-recently-modified files in a directory. Superseded by the
  richer `generate_patch` in `spec_diff.py`.</docstring>
    <context>
      <feature_name>UtilityFunctions</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>`libspec.util` provides a handful of pure utility helpers used
internally across the library.

- `fqn(obj)`: Returns the fully qualified name of a class or instance
  as "module.qualname", used as the canonical XML ref attribute.

- `easy_hash(text)`: Returns the MD5 hex digest of a UTF-8 string,
  used to compute the content-addressed XML filename.

- `get_libspec_version()`: Returns the installed package version via
  `importlib.metadata`, falling back to "unknown" if unavailable.

- `diff_two_latest(dirpath)`: (Legacy) Returns a unified diff of the
  two most-recently-modified files in a directory. Superseded by the
  richer `generate_patch` in `spec_diff.py`.</notes>
      <feature_name>UtilityFunctions</feature_name>
    </delta_requirements>
  </specification>
  <specification type="ConcurrentInitSafety" ref="spec.lsp_auto_init.ConcurrentInitSafety">
    <source target="ConcurrentInitSafety" file="/usr/backup-working/work/libspec/spec/lsp_auto_init.py"/>
    <docstring>The auto-initialization logic must be thread-safe or 
guarantee atomicity to prevent multiple rapid tool calls from 
spawning redundant LSP processes.</docstring>
    <context>
      <req_id>spec.lsp_auto_init.ConcurrentInitSafety</req_id>
      <title>ConcurrentInitSafety</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.lsp_auto_init.ConcurrentInitSafety</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The auto-initialization logic must be thread-safe or 
guarantee atomicity to prevent multiple rapid tool calls from 
spawning redundant LSP processes.</notes>
      <req_id>spec.lsp_auto_init.ConcurrentInitSafety</req_id>
      <title>ConcurrentInitSafety</title>
    </delta_requirements>
  </specification>
  <specification type="DiagnosticInitialization" ref="spec.lsp_auto_init.DiagnosticInitialization">
    <source target="DiagnosticInitialization" file="/usr/backup-working/work/libspec/spec/lsp_auto_init.py"/>
    <docstring>If auto-initialization fails (e.g., due to missing environment 
dependencies like `pylsp`), the failure must follow the Story-Driven 
Error pattern defined in `Err`.

The error message should:
- Identify that an auto-start was attempted.
- Explain exactly why it failed (e.g., "pylsp not found in PATH").
- Provide a clear remediation step (e.g., "Run `uv add --dev python-lsp-server`").</docstring>
    <context>
      <feature_name>DiagnosticInitialization</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>If auto-initialization fails (e.g., due to missing environment 
dependencies like `pylsp`), the failure must follow the Story-Driven 
Error pattern defined in `Err`.

The error message should:
- Identify that an auto-start was attempted.
- Explain exactly why it failed (e.g., "pylsp not found in PATH").
- Provide a clear remediation step (e.g., "Run `uv add --dev python-lsp-server`").</notes>
      <feature_name>DiagnosticInitialization</feature_name>
    </delta_requirements>
  </specification>
  <specification type="LazyLspStart" ref="spec.lsp_auto_init.LazyLspStart">
    <source target="LazyLspStart" file="/usr/backup-working/work/libspec/spec/lsp_auto_init.py"/>
    <docstring>Any tool that depends on the background LSP process must 
perform a "lazy check" on the LSP state.

1. If the LSP process is not running, the tool must automatically 
   invoke the initialization sequence before proceeding.
2. The initialization must use sensible defaults (e.g., searching 
   for a `spec/` directory or using the current workspace root).
3. This process must be transparent to the caller.</docstring>
    <context>
      <feature_name>LazyLspStart</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Any tool that depends on the background LSP process must 
perform a "lazy check" on the LSP state.

1. If the LSP process is not running, the tool must automatically 
   invoke the initialization sequence before proceeding.
2. The initialization must use sensible defaults (e.g., searching 
   for a `spec/` directory or using the current workspace root).
3. This process must be transparent to the caller.</notes>
      <feature_name>LazyLspStart</feature_name>
    </delta_requirements>
  </specification>
  <specification type="LspAutoInit" ref="spec.lsp_auto_init.LspAutoInit">
    <source target="LspAutoInit" file="/usr/backup-working/work/libspec/spec/lsp_auto_init.py"/>
    <docstring>The libspec MCP server must eliminate manual lifecycle management 
for the background LSP server. 

The current requirement that a user or agent must manually call 
`libspec_start_lsp` before performing semantic searches creates 
unnecessary cognitive load and friction in agentic workflows.</docstring>
    <context>
      <req_id>spec.lsp_auto_init.LspAutoInit</req_id>
      <title>LspAutoInit</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.lsp_auto_init.LspAutoInit</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The libspec MCP server must eliminate manual lifecycle management 
for the background LSP server. 

The current requirement that a user or agent must manually call 
`libspec_start_lsp` before performing semantic searches creates 
unnecessary cognitive load and friction in agentic workflows.</notes>
      <req_id>spec.lsp_auto_init.LspAutoInit</req_id>
      <title>LspAutoInit</title>
    </delta_requirements>
  </specification>
  <specification type="HelloPlugin" ref="spec.hello_plugin.HelloPlugin">
    <source target="HelloPlugin" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>A minimal project-local pylsp plugin that says hello to every file
in the workspace.

HelloPlugin is the canonical example plugin. It ships as a template
at `.libspec/plugins/hello_plugin.py` inside the project being served,
NOT as a module bundled inside the libspec package.

The plugin should:
1. Hook into `pylsp_document_did_open`.
2. Log a "Hello" message identifying the file being opened.
3. Support dynamic enable/disable via the `libspec_pylsp_plugin` MCP tool.</docstring>
    <context>
      <feature_name>HelloPlugin</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>A minimal project-local pylsp plugin that says hello to every file
in the workspace.

HelloPlugin is the canonical example plugin. It ships as a template
at `.libspec/plugins/hello_plugin.py` inside the project being served,
NOT as a module bundled inside the libspec package.

The plugin should:
1. Hook into `pylsp_document_did_open`.
2. Log a "Hello" message identifying the file being opened.
3. Support dynamic enable/disable via the `libspec_pylsp_plugin` MCP tool.</notes>
      <feature_name>HelloPlugin</feature_name>
    </delta_requirements>
  </specification>
  <specification type="PluginLoaderInit" ref="spec.hello_plugin.PluginLoaderInit">
    <source target="PluginLoaderInit" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>The plugin loader must be triggered on `pylsp_initialize` so that
all project-local plugins are registered before any document events fire.

The workspace root is resolved from the LSP `rootUri` parameter passed
to `pylsp_initialize`.</docstring>
    <context>
      <feature_name>PluginLoaderInit</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The plugin loader must be triggered on `pylsp_initialize` so that
all project-local plugins are registered before any document events fire.

The workspace root is resolved from the LSP `rootUri` parameter passed
to `pylsp_initialize`.</notes>
      <feature_name>PluginLoaderInit</feature_name>
    </delta_requirements>
  </specification>
  <specification type="PluginMcpControl" ref="spec.hello_plugin.PluginMcpControl">
    <source target="PluginMcpControl" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>The `libspec_pylsp_plugin` MCP tool allows the agent to interact with
any pylsp plugin by name.

Parameters:
- plugin_name (str): The name of the plugin to control (e.g., "hello", "pyflakes").
- action (str, optional): The action to perform (e.g., "status", "enable",
  "disable"). If omitted, defaults to "status".

Returns a JSON-formatted string indicating the current status of the
requested plugin or the result of the requested action.</docstring>
    <context>
      <feature_name>PluginMcpControl</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_pylsp_plugin` MCP tool allows the agent to interact with
any pylsp plugin by name.

Parameters:
- plugin_name (str): The name of the plugin to control (e.g., "hello", "pyflakes").
- action (str, optional): The action to perform (e.g., "status", "enable",
  "disable"). If omitted, defaults to "status".

Returns a JSON-formatted string indicating the current status of the
requested plugin or the result of the requested action.</notes>
      <feature_name>PluginMcpControl</feature_name>
    </delta_requirements>
  </specification>
  <specification type="ProjectLocalPlugins" ref="spec.hello_plugin.ProjectLocalPlugins">
    <source target="ProjectLocalPlugins" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>libspec must support loading project-local pylsp plugins from
`&lt;project-root&gt;/.libspec/plugins/`.

This is the core mechanism that allows a project to ship its own
pylsp diagnostics, linters, and hovers without modifying the global
libspec package.

The loading strategy must NOT rely on entry points for per-file
discovery, since entry points require package installation. Instead,
the existing registered libspec pylsp plugin (`libspec.pylsp_plugin`)
acts as a "loader" that dynamically imports and registers project-local
plugins at pylsp startup time.

Loading sequence:
1. On `pylsp_initialize`, discover all `*.py` files in
   `&lt;workspace-root&gt;/.libspec/plugins/`.
2. Add `.libspec/plugins/` to `sys.path` if not already present.
3. `importlib.import_module` each discovered file by its stem name.
4. Call `workspace._config._plugin_manager.register(module)` to register
   each module as a pluggy plugin.

If a file fails to import or register, a descriptive warning must be
logged; it must not crash the LSP server.</docstring>
    <context>
      <req_id>spec.hello_plugin.ProjectLocalPlugins</req_id>
      <title>ProjectLocalPlugins</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.hello_plugin.ProjectLocalPlugins</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>libspec must support loading project-local pylsp plugins from
`&lt;project-root&gt;/.libspec/plugins/`.

This is the core mechanism that allows a project to ship its own
pylsp diagnostics, linters, and hovers without modifying the global
libspec package.

The loading strategy must NOT rely on entry points for per-file
discovery, since entry points require package installation. Instead,
the existing registered libspec pylsp plugin (`libspec.pylsp_plugin`)
acts as a "loader" that dynamically imports and registers project-local
plugins at pylsp startup time.

Loading sequence:
1. On `pylsp_initialize`, discover all `*.py` files in
   `&lt;workspace-root&gt;/.libspec/plugins/`.
2. Add `.libspec/plugins/` to `sys.path` if not already present.
3. `importlib.import_module` each discovered file by its stem name.
4. Call `workspace._config._plugin_manager.register(module)` to register
   each module as a pluggy plugin.

If a file fails to import or register, a descriptive warning must be
logged; it must not crash the LSP server.</notes>
      <req_id>spec.hello_plugin.ProjectLocalPlugins</req_id>
      <title>ProjectLocalPlugins</title>
    </delta_requirements>
  </specification>
  <specification type="ASTParsing" ref="spec.hello_ast.ASTParsing">
    <source target="ASTParsing" file="/usr/backup-working/work/libspec/spec/hello_ast.py"/>
    <docstring>The plugin must use the `ast` module to traverse the source tree and extract identifiers.</docstring>
    <context>
      <req_id>spec.hello_ast.ASTParsing</req_id>
      <title>ASTParsing</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.hello_ast.ASTParsing</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The plugin must use the `ast` module to traverse the source tree and extract identifiers.</notes>
      <req_id>spec.hello_ast.ASTParsing</req_id>
      <title>ASTParsing</title>
    </delta_requirements>
  </specification>
  <specification type="HelloAST" ref="spec.hello_ast.HelloAST">
    <source target="HelloAST" file="/usr/backup-working/work/libspec/spec/hello_ast.py"/>
    <docstring>HelloAST — A project-local pylsp plugin for regex-based identifier discovery.

This plugin leverages the Python `ast` module to find all identifiers 
(variables, function names, class names) that match a specific regex.</docstring>
    <context>
      <feature_name>HelloASTIdentifierSearch</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>HelloAST — A project-local pylsp plugin for regex-based identifier discovery.

This plugin leverages the Python `ast` module to find all identifiers 
(variables, function names, class names) that match a specific regex.</notes>
      <feature_name>HelloASTIdentifierSearch</feature_name>
    </delta_requirements>
  </specification>
  <specification type="PluginDiscovery" ref="spec.hello_ast.PluginDiscovery">
    <source target="PluginDiscovery" file="/usr/backup-working/work/libspec/spec/hello_ast.py"/>
    <docstring>The plugin must be located at `.libspec/plugins/hello_ast.py` to be automatically loaded.</docstring>
    <context>
      <req_id>spec.hello_ast.PluginDiscovery</req_id>
      <title>PluginDiscovery</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.hello_ast.PluginDiscovery</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The plugin must be located at `.libspec/plugins/hello_ast.py` to be automatically loaded.</notes>
      <req_id>spec.hello_ast.PluginDiscovery</req_id>
      <title>PluginDiscovery</title>
    </delta_requirements>
  </specification>
  <specification type="RegexConfiguration" ref="spec.hello_ast.RegexConfiguration">
    <source target="RegexConfiguration" file="/usr/backup-working/work/libspec/spec/hello_ast.py"/>
    <docstring>The plugin must support a `pattern` setting via pylsp configuration.</docstring>
    <context>
      <req_id>spec.hello_ast.RegexConfiguration</req_id>
      <title>RegexConfiguration</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.hello_ast.RegexConfiguration</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The plugin must support a `pattern` setting via pylsp configuration.</notes>
      <req_id>spec.hello_ast.RegexConfiguration</req_id>
      <title>RegexConfiguration</title>
    </delta_requirements>
  </specification>
  <specification type="WorkspaceScanning" ref="spec.hello_ast.WorkspaceScanning">
    <source target="WorkspaceScanning" file="/usr/backup-working/work/libspec/spec/hello_ast.py"/>
    <docstring>The plugin must be able to scan the entire workspace when triggered.</docstring>
    <context>
      <req_id>spec.hello_ast.WorkspaceScanning</req_id>
      <title>WorkspaceScanning</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.hello_ast.WorkspaceScanning</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>The plugin must be able to scan the entire workspace when triggered.</notes>
      <req_id>spec.hello_ast.WorkspaceScanning</req_id>
      <title>WorkspaceScanning</title>
    </delta_requirements>
  </specification>
  <specification type="CurrentSnapshot" ref="spec.store.CurrentSnapshot">
    <source target="CurrentSnapshot" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to retrieve the latest active metadata snapshot from the store.

The operation must:
- Read the metadata of the most recent build session.
- Return a `Snapshot` instance, or `None` if the store does not contain any builds.</docstring>
    <context>
      <req_id>spec.store.CurrentSnapshot</req_id>
      <title>CurrentSnapshot</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.CurrentSnapshot</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to retrieve the latest active metadata snapshot from the store.

The operation must:
- Read the metadata of the most recent build session.
- Return a `Snapshot` instance, or `None` if the store does not contain any builds.</notes>
      <req_id>spec.store.CurrentSnapshot</req_id>
      <title>CurrentSnapshot</title>
    </delta_requirements>
  </specification>
  <specification type="GetComponent" ref="spec.store.GetComponent">
    <source target="GetComponent" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to retrieve a specific component's definition from the current active snapshot.

The operation must:
- Lookup the component by its dot-separated FQN reference.
- Return a fully populated `Component` object.
- Raise `StoreNotFoundError` if the reference does not exist in the active snapshot.</docstring>
    <context>
      <req_id>spec.store.GetComponent</req_id>
      <title>GetComponent</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.GetComponent</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to retrieve a specific component's definition from the current active snapshot.

The operation must:
- Lookup the component by its dot-separated FQN reference.
- Return a fully populated `Component` object.
- Raise `StoreNotFoundError` if the reference does not exist in the active snapshot.</notes>
      <req_id>spec.store.GetComponent</req_id>
      <title>GetComponent</title>
    </delta_requirements>
  </specification>
  <specification type="ListComponents" ref="spec.store.ListComponents">
    <source target="ListComponents" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to list all components associated with the latest active build.

The operation must:
- Return a list of all `Component` objects in the active snapshot.
- Return an empty list if no snapshot has been stored.</docstring>
    <context>
      <req_id>spec.store.ListComponents</req_id>
      <title>ListComponents</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.ListComponents</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to list all components associated with the latest active build.

The operation must:
- Return a list of all `Component` objects in the active snapshot.
- Return an empty list if no snapshot has been stored.</notes>
      <req_id>spec.store.ListComponents</req_id>
      <title>ListComponents</title>
    </delta_requirements>
  </specification>
  <specification type="ListImplemented" ref="spec.store.ListImplemented">
    <source target="ListImplemented" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to list all active implementation claims scoped to a specific snapshot boundary.

The operation must:
- Accept a target `Snapshot` instance.
- Filter and return all `Implemented` records matching that snapshot's active scope.</docstring>
    <context>
      <req_id>spec.store.ListImplemented</req_id>
      <title>ListImplemented</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.ListImplemented</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to list all active implementation claims scoped to a specific snapshot boundary.

The operation must:
- Accept a target `Snapshot` instance.
- Filter and return all `Implemented` records matching that snapshot's active scope.</notes>
      <req_id>spec.store.ListImplemented</req_id>
      <title>ListImplemented</title>
    </delta_requirements>
  </specification>
  <specification type="PostgreSQLStore" ref="spec.store.PostgreSQLStore">
    <source target="PostgreSQLStore" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Centralized relational storage engine using PostgreSQL for distributed engineering teams.

The engine must:
- Map the exact same Peewee schemas natively to a remote PostgreSQL server.
- Synchronize team-wide builds in real-time, allowing developers to pull claims and specs instantly.
- Support co-existent hybrid merging with read-only SQLite files from upstream dependency packages.</docstring>
    <context>
      <feature_name>PostgreSQLStore</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Centralized relational storage engine using PostgreSQL for distributed engineering teams.

The engine must:
- Map the exact same Peewee schemas natively to a remote PostgreSQL server.
- Synchronize team-wide builds in real-time, allowing developers to pull claims and specs instantly.
- Support co-existent hybrid merging with read-only SQLite files from upstream dependency packages.</notes>
      <feature_name>PostgreSQLStore</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SQLiteStore" ref="spec.store.SQLiteStore">
    <source target="SQLiteStore" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Relational storage engine using SQLite via Peewee.

The engine must:
- Support fully normalized tables: Build (pruned to 2 latest), Spec, Edge (flat MRO), and Implemented.
- Execute compiler builds and verification queries with sub-millisecond local latencies.
- Treat the database file as a derived, uncommitted compile target built on demand via CLI or MCP.</docstring>
    <context>
      <feature_name>SQLiteStore</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Relational storage engine using SQLite via Peewee.

The engine must:
- Support fully normalized tables: Build (pruned to 2 latest), Spec, Edge (flat MRO), and Implemented.
- Execute compiler builds and verification queries with sub-millisecond local latencies.
- Treat the database file as a derived, uncommitted compile target built on demand via CLI or MCP.</notes>
      <feature_name>SQLiteStore</feature_name>
    </delta_requirements>
  </specification>
  <specification type="SpecComponent" ref="spec.store.SpecComponent">
    <source target="SpecComponent" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Immutable data structure representing a compiled specification node.

Fields:
- `ref` (str): Dot-separated unique reference path to the specification class.
- `docstring` (str): Fully rendered English prose, with MRO bases and Jinja variables resolved.
- `is_template` (bool): True if the specification is a template containing placeholders.
- `inherits` (list[str]): Ancestral specification FQNs ordered in strict Method Resolution Order (MRO).
- `hash` (str): SHA-256 fingerprint of the fully rendered docstring string.</docstring>
    <context>
      <req_id>spec.store.SpecComponent</req_id>
      <title>SpecComponent</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.SpecComponent</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Immutable data structure representing a compiled specification node.

Fields:
- `ref` (str): Dot-separated unique reference path to the specification class.
- `docstring` (str): Fully rendered English prose, with MRO bases and Jinja variables resolved.
- `is_template` (bool): True if the specification is a template containing placeholders.
- `inherits` (list[str]): Ancestral specification FQNs ordered in strict Method Resolution Order (MRO).
- `hash` (str): SHA-256 fingerprint of the fully rendered docstring string.</notes>
      <req_id>spec.store.SpecComponent</req_id>
      <title>SpecComponent</title>
    </delta_requirements>
  </specification>
  <specification type="SpecImplemented" ref="spec.store.SpecImplemented">
    <source target="SpecImplemented" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Immutable structure representing an agent's claim of a satisfying implementation.

Fields:
- `ref` (str): Reference string of the specification that was implemented.
- `spec_hash` (str): The specification's docstring hash at the exact time the code was written.
- `file` (str): Workspace filesystem path to the file containing the `# IMPLEMENTS` marker comment.
- `line` (int): Line number of the injected `# IMPLEMENTS` marker comment.
- `session_id` (str | None): Active agent session identifier tracking the context of implementation.</docstring>
    <context>
      <req_id>spec.store.SpecImplemented</req_id>
      <title>SpecImplemented</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.SpecImplemented</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Immutable structure representing an agent's claim of a satisfying implementation.

Fields:
- `ref` (str): Reference string of the specification that was implemented.
- `spec_hash` (str): The specification's docstring hash at the exact time the code was written.
- `file` (str): Workspace filesystem path to the file containing the `# IMPLEMENTS` marker comment.
- `line` (int): Line number of the injected `# IMPLEMENTS` marker comment.
- `session_id` (str | None): Active agent session identifier tracking the context of implementation.</notes>
      <req_id>spec.store.SpecImplemented</req_id>
      <title>SpecImplemented</title>
    </delta_requirements>
  </specification>
  <specification type="SpecSnapshot" ref="spec.store.SpecSnapshot">
    <source target="SpecSnapshot" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Immutable metadata structure representing a discrete compile build instance.

Fields:
- `id` (str): Unique alphanumeric identifier derived from the master hash.
- `created_at` (datetime): Timezone-aware timestamp indicating when the build was compiled.
- `master_hash` (str): SHA-256 fingerprint computed deterministically from sorted child component hashes.
- `git_commit` (str | None): Active 40-character git commit SHA-1 of the repository at build time.</docstring>
    <context>
      <req_id>spec.store.SpecSnapshot</req_id>
      <title>SpecSnapshot</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.SpecSnapshot</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Immutable metadata structure representing a discrete compile build instance.

Fields:
- `id` (str): Unique alphanumeric identifier derived from the master hash.
- `created_at` (datetime): Timezone-aware timestamp indicating when the build was compiled.
- `master_hash` (str): SHA-256 fingerprint computed deterministically from sorted child component hashes.
- `git_commit` (str | None): Active 40-character git commit SHA-1 of the repository at build time.</notes>
      <req_id>spec.store.SpecSnapshot</req_id>
      <title>SpecSnapshot</title>
    </delta_requirements>
  </specification>
  <specification type="SpecStoreProtocol" ref="spec.store.SpecStoreProtocol">
    <source target="SpecStoreProtocol" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Backend-agnostic interface boundary establishing the data access operations.
The compiler core (SpecEngine) interacts exclusively with this Protocol, decoupled from storage mechanisms.</docstring>
    <context>
      <req_id>spec.store.SpecStoreProtocol</req_id>
      <title>SpecStoreProtocol</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.SpecStoreProtocol</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Backend-agnostic interface boundary establishing the data access operations.
The compiler core (SpecEngine) interacts exclusively with this Protocol, decoupled from storage mechanisms.</notes>
      <req_id>spec.store.SpecStoreProtocol</req_id>
      <title>SpecStoreProtocol</title>
    </delta_requirements>
  </specification>
  <specification type="StoreCorruptedDataError" ref="spec.store.StoreCorruptedDataError">
    <source target="StoreCorruptedDataError" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Raised when data verification, deserialization, or rendered docstring formatting fails.</docstring>
    <context>
      <req_id>spec.store.StoreCorruptedDataError</req_id>
      <title>StoreCorruptedDataError</title>
    </context>
    <inherits>
      <ref>spec.store.StoreError</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreCorruptedDataError</id>
      <id>spec.store.StoreError</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Raised when data verification, deserialization, or rendered docstring formatting fails.</notes>
      <req_id>spec.store.StoreCorruptedDataError</req_id>
      <title>StoreCorruptedDataError</title>
    </delta_requirements>
  </specification>
  <specification type="StoreError" ref="spec.store.StoreError">
    <source target="StoreError" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Root ancestral exception class for all errors originating from the storage layer.
All backend-specific database or file exceptions must be caught and raised as a sub-class of StoreError.</docstring>
    <context>
      <req_id>spec.store.StoreError</req_id>
      <title>StoreError</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreError</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Root ancestral exception class for all errors originating from the storage layer.
All backend-specific database or file exceptions must be caught and raised as a sub-class of StoreError.</notes>
      <req_id>spec.store.StoreError</req_id>
      <title>StoreError</title>
    </delta_requirements>
  </specification>
  <specification type="StoreIOError" ref="spec.store.StoreIOError">
    <source target="StoreIOError" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Raised when reading from or writing to the underlying database, remote host, or filesystem fails.</docstring>
    <context>
      <req_id>spec.store.StoreIOError</req_id>
      <title>StoreIOError</title>
    </context>
    <inherits>
      <ref>spec.store.StoreError</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreIOError</id>
      <id>spec.store.StoreError</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Raised when reading from or writing to the underlying database, remote host, or filesystem fails.</notes>
      <req_id>spec.store.StoreIOError</req_id>
      <title>StoreIOError</title>
    </delta_requirements>
  </specification>
  <specification type="StoreImplemented" ref="spec.store.StoreImplemented">
    <source target="StoreImplemented" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to record a code-level implementation claim.

The operation must:
- Accept a single `Implemented` dataclass log record.
- Persist the record, enforcing a strict one-claim-per-invocation atomic boundary.
- Raise `StoreIOError` on persistence failures.</docstring>
    <context>
      <req_id>spec.store.StoreImplemented</req_id>
      <title>StoreImplemented</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreImplemented</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to record a code-level implementation claim.

The operation must:
- Accept a single `Implemented` dataclass log record.
- Persist the record, enforcing a strict one-claim-per-invocation atomic boundary.
- Raise `StoreIOError` on persistence failures.</notes>
      <req_id>spec.store.StoreImplemented</req_id>
      <title>StoreImplemented</title>
    </delta_requirements>
  </specification>
  <specification type="StoreNotFoundError" ref="spec.store.StoreNotFoundError">
    <source target="StoreNotFoundError" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Raised when a requested snapshot or component reference is not present in the current build context.</docstring>
    <context>
      <req_id>spec.store.StoreNotFoundError</req_id>
      <title>StoreNotFoundError</title>
    </context>
    <inherits>
      <ref>spec.store.StoreError</ref>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreNotFoundError</id>
      <id>spec.store.StoreError</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Raised when a requested snapshot or component reference is not present in the current build context.</notes>
      <req_id>spec.store.StoreNotFoundError</req_id>
      <title>StoreNotFoundError</title>
    </delta_requirements>
  </specification>
  <specification type="StoreSnapshot" ref="spec.store.StoreSnapshot">
    <source target="StoreSnapshot" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Operation to atomically save a full compiled tree of components under a new active snapshot.

The operation must:
- Accept a list of `Component` objects and an optional git commit string.
- Compute the snapshot's deterministic master_hash by sorting component hashes alphabetically by ref.
- Return a frozen `Snapshot` instance representing the completed transaction.
- Raise `StoreIOError` if the underlying write to database or disk fails.</docstring>
    <context>
      <req_id>spec.store.StoreSnapshot</req_id>
      <title>StoreSnapshot</title>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Requirement</ref>
    </inherits>
    <effective_req_ids>
      <id>spec.store.StoreSnapshot</id>
      <id>spec.err.Req</id>
    </effective_req_ids>
    <delta_requirements>
      <notes>Operation to atomically save a full compiled tree of components under a new active snapshot.

The operation must:
- Accept a list of `Component` objects and an optional git commit string.
- Compute the snapshot's deterministic master_hash by sorting component hashes alphabetically by ref.
- Return a frozen `Snapshot` instance representing the completed transaction.
- Raise `StoreIOError` if the underlying write to database or disk fails.</notes>
      <req_id>spec.store.StoreSnapshot</req_id>
      <title>StoreSnapshot</title>
    </delta_requirements>
  </specification>
  <specification type="XmlStoreAdapter" ref="spec.store.XmlStoreAdapter">
    <source target="XmlStoreAdapter" file="/usr/backup-working/work/libspec/spec/store.py"/>
    <docstring>Strangler Fig passive adapter translating Legacy XML files to the SpecStore interface.

The adapter must:
- Load and parse legacy XML files under the new `SpecStoreProtocol` API.
- Preserve 100% format compatibility with existing XML specification formats.
- Perform all file updates atomically using `os.replace` to prevent file corruption during crashes.</docstring>
    <context>
      <feature_name>XmlStoreAdapter</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>Strangler Fig passive adapter translating Legacy XML files to the SpecStore interface.

The adapter must:
- Load and parse legacy XML files under the new `SpecStoreProtocol` API.
- Preserve 100% format compatibility with existing XML specification formats.
- Perform all file updates atomically using `os.replace` to prevent file corruption during crashes.</notes>
      <feature_name>XmlStoreAdapter</feature_name>
    </delta_requirements>
  </specification>
  <specification type="Err" ref="spec.err.Err" dependency="true" template="false">
    <source target="Err" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>It is important that error handling be done excellently. If a
function can fail, then it needs to do so in the most elegant way
possible. Error reporting, handling, exceptions and all aspects
of failure must be taken to extreme. It should be possible to
understand the program by reading the error messages.

When an error occurs there should be a story about the failure at
each step of the way. What went wrong and why.</docstring_template>
  </specification>
  <specification type="Refactor" ref="spec.err.Refactor" dependency="true" template="false">
    <source target="Refactor" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Always keep an eye out for ways to generalize a function if it's utility
might be helpful to other functions.

Classes should be implemented in their own files with filename
being the classname with correct naming convention</docstring_template>
    <inherits>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
    </inherits>
  </specification>
  <specification type="BoilerPlate" ref="spec.err.BoilerPlate" dependency="true" template="false">
    <source target="BoilerPlate" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>If you can see a way to reduce boiler plate, then do it.</docstring_template>
  </specification>
  <specification type="FunctionLines" ref="spec.err.FunctionLines" dependency="true" template="false">
    <source target="FunctionLines" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Try to keep functions under 20 lines.</docstring_template>
  </specification>
  <specification type="Indentation" ref="spec.err.Indentation" dependency="true" template="false">
    <source target="Indentation" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Try to keep indentation under 4 levels</docstring_template>
  </specification>
  <specification type="Robustness" ref="spec.err.Robustness" dependency="true" template="false">
    <source target="Robustness" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Always prioritize library-provided constructors for complex objects.
Ensure all components are fully initialized before calling any state-mutating methods.
Assume private internal state is uninitialized until the official
constructor has returned.
When extending library components, prioritize composition (pointers) 
over embedding by value to avoid risky state-copying bugs.

Use dependency injection for system level objects for
composability and to make testing easier.</docstring_template>
    <inherits>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
    </inherits>
  </specification>
  <specification type="PreCondition" ref="spec.err.PreCondition" dependency="true" template="false">
    <source target="PreCondition" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Functions should validate preconditions at their entry point. Instead of 
using `assert` statements (which can be disabled globally), raise explicit, 
descriptive exceptions (e.g., ValueError, TypeError, or custom domain exceptions) 
to robustly reject malformed input.</docstring_template>
  </specification>
  <specification type="PostCondition" ref="spec.err.PostCondition" dependency="true" template="false">
    <source target="PostCondition" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Before a function returns, it should verify postconditions to ensure 
invariant properties hold true. Raise explicit, descriptive exceptions 
(such as RuntimeError or domain exceptions) rather than using `assert` 
statements to handle post-execution verification failures.</docstring_template>
  </specification>
  <specification type="GlobalMutableState" ref="spec.err.GlobalMutableState" dependency="true" template="false">
    <source target="GlobalMutableState" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Broadly you should avoid global mutable state.</docstring_template>
  </specification>
  <specification type="Requirement" ref="libspec.spec_types.Requirement" dependency="true" template="true">
    <source target="Requirement" file="/usr/backup-working/work/libspec/libspec/spec_types.py"/>
    <docstring_template>Requirement
TITLE: {{title}}
REQUIREMENT-ID: {{req_id}}

Insert REQUIREMENT-ID into any source code for cross reference purposes.</docstring_template>
  </specification>
  <specification type="Feature" ref="libspec.spec_types.Feature" dependency="true" template="true">
    <source target="Feature" file="/usr/backup-working/work/libspec/libspec/spec_types.py"/>
    <docstring_template>Feature Specification: {{feature_name}}</docstring_template>
  </specification>
</specification_set>
