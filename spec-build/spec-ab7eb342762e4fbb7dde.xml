<?xml version="1.0" ?>
<specification_set date-created="2026-05-13T21:14:15.492157-07:00" libspec-version="4.0.1">
  <specification type="HelloPlugin" ref="spec.hello_plugin.HelloPlugin">
    <source target="HelloPlugin" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>A pylsp plugin that provides a friendly greeting for specification files.

The plugin should:
1. Identify files within the `spec/` directory.
2. Provide a diagnostic (lint) message at the top of these files saying
   "Hi from HelloPlugin!".
3. Support dynamic configuration/control via the MCP server.</docstring>
    <context>
      <feature_name>HelloPlugin</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>A pylsp plugin that provides a friendly greeting for specification files.

The plugin should:
1. Identify files within the `spec/` directory.
2. Provide a diagnostic (lint) message at the top of these files saying
   "Hi from HelloPlugin!".
3. Support dynamic configuration/control via the MCP server.</notes>
      <feature_name>HelloPlugin</feature_name>
    </delta_requirements>
  </specification>
  <specification type="HelloPluginMcpControl" ref="spec.hello_plugin.HelloPluginMcpControl">
    <source target="HelloPluginMcpControl" file="/usr/backup-working/work/libspec/spec/hello_plugin.py"/>
    <docstring>The `libspec_hello_plugin` MCP tool allows the agent to interact with
the HelloPlugin.

Parameters:
- action (str, optional): The action to perform (e.g., "status", "enable",
  "disable"). If omitted, defaults to "status".

Returns a JSON-formatted string indicating the current status of the
HelloPlugin or the result of the requested action.</docstring>
    <context>
      <feature_name>HelloPluginMcpControl</feature_name>
    </context>
    <inherits>
      <ref>spec.err.Err</ref>
      <ref>spec.err.Refactor</ref>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
      <ref>spec.err.Robustness</ref>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
      <ref>libspec.spec_types.Feature</ref>
    </inherits>
    <delta_requirements>
      <notes>The `libspec_hello_plugin` MCP tool allows the agent to interact with
the HelloPlugin.

Parameters:
- action (str, optional): The action to perform (e.g., "status", "enable",
  "disable"). If omitted, defaults to "status".

Returns a JSON-formatted string indicating the current status of the
HelloPlugin or the result of the requested action.</notes>
      <feature_name>HelloPluginMcpControl</feature_name>
    </delta_requirements>
  </specification>
  <specification type="Err" ref="spec.err.Err" dependency="true" template="false">
    <source target="Err" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>It is important that error handling be done excellently. If a
function can fail, then it needs to do so in the most elegant way
possible. Error reporting, handling, exceptions and all aspects
of failure must be taken to extreme. It should be possible to
understand the program by reading the error messages.

When an error occurs there should be a story about the failure at
each step of the way. What went wrong and why.</docstring_template>
  </specification>
  <specification type="Refactor" ref="spec.err.Refactor" dependency="true" template="false">
    <source target="Refactor" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Always keep an eye out for ways to generalize a function if it's utility
might be helpful to other functions.

Classes should be implemented in their own files with filename
being the classname with correct naming convention</docstring_template>
    <inherits>
      <ref>spec.err.BoilerPlate</ref>
      <ref>spec.err.FunctionLines</ref>
      <ref>spec.err.Indentation</ref>
    </inherits>
  </specification>
  <specification type="BoilerPlate" ref="spec.err.BoilerPlate" dependency="true" template="false">
    <source target="BoilerPlate" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>If you can see a way to reduce boiler plate, then do it.</docstring_template>
  </specification>
  <specification type="FunctionLines" ref="spec.err.FunctionLines" dependency="true" template="false">
    <source target="FunctionLines" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Try to keep functions under 20 lines.</docstring_template>
  </specification>
  <specification type="Indentation" ref="spec.err.Indentation" dependency="true" template="false">
    <source target="Indentation" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Try to keep indentation under 4 levels</docstring_template>
  </specification>
  <specification type="Robustness" ref="spec.err.Robustness" dependency="true" template="false">
    <source target="Robustness" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Always prioritize library-provided constructors for complex objects.
Ensure all components are fully initialized before calling any state-mutating methods.
Assume private internal state is uninitialized until the official
constructor has returned.
When extending library components, prioritize composition (pointers) 
over embedding by value to avoid risky state-copying bugs.

Use dependency injection for system level objects for
composability and to make testing easier.</docstring_template>
    <inherits>
      <ref>spec.err.PreCondition</ref>
      <ref>spec.err.PostCondition</ref>
      <ref>spec.err.GlobalMutableState</ref>
    </inherits>
  </specification>
  <specification type="PreCondition" ref="spec.err.PreCondition" dependency="true" template="false">
    <source target="PreCondition" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Functions should start with preconditions about what should be
true. There should be `assert` statments to enforce this</docstring_template>
  </specification>
  <specification type="PostCondition" ref="spec.err.PostCondition" dependency="true" template="false">
    <source target="PostCondition" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Before a function returns, it should use assert statements to
verify certain important properties are true</docstring_template>
  </specification>
  <specification type="GlobalMutableState" ref="spec.err.GlobalMutableState" dependency="true" template="false">
    <source target="GlobalMutableState" file="/usr/backup-working/work/libspec/spec/err.py"/>
    <docstring_template>Broadly you should avoid global mutable state.</docstring_template>
  </specification>
  <specification type="Feature" ref="libspec.spec_types.Feature" dependency="true" template="true">
    <source target="Feature" file="/usr/backup-working/work/libspec/libspec/spec_types.py"/>
    <docstring_template>Feature Specification: {{feature_name}}</docstring_template>
  </specification>
</specification_set>
