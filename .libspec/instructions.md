This project uses libspec for specification-driven development.

Before writing any code, run:
  uv run libspec repl --json diff

To understand the full workflow:
  uv run libspec skills

To see all available commands:
  uv run libspec tools
